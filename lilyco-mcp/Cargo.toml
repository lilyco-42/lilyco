[package]
name = "lilyco-mcp"
version = "0.2.2"
edition = "2021"
description = "Lilyco MCP server — expose Lilyco commands as Model Context Protocol tools over stdio"
license = "MIT OR Apache-2.0"
repository = "https://github.com/lilyco-42/lilyco"
keywords = ["mcp", "ai", "llm", "tools", "protocol"]
categories = ["development-tools"]

[dependencies]
lilyco-core = { version = "0.2", path = "../lilyco-core" }
serde_json = "1"

[package]
name = "triforge-tui"
version = "0.1.0"
edition = "2021"
description = "Triforge TUI 渲染器 —— 把 CommandSchema 转换成交互式终端表单"

[dependencies]
triforge-core = { path = "../triforge-core" }
ratatui = "0.29"
crossterm = "0.28"
serde_json = "1"

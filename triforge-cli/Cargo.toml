[package]
name = "triforge-cli"
version = "0.1.0"
edition = "2021"
description = "Triforge CLI 渲染器 —— 把 CommandSchema 转换成 clap::Command"

[dependencies]
triforge-core = { path = "../triforge-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"

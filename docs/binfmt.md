# lbin — 二进制 / 容器结构查看使用文档

`lilyco-binfmt` 是 lilyco 框架的**第二个纯只读域二进制**：一个 `lbin` 挂「看一个文件到底是什么结构」
这一整域的 13 条命令，照 `lilyco-files` 的样板获得 **CLI / TUI / Web / MCP** 四端 + AI 可调用。

- 仓库：`https://github.com/lilyco-42/lilyco`
- 二进制名：`lbin`
- 依赖：`object`（符号层）+ 自己写的字节走查（识别 / 成员表 / 分区图），**不执行、不解压、不写盘**
- 安全级：**13 条命令全是 T0 只读**，MCP 的 `DenyElevated` 策略下照样放行

---

## 安装

```bash
cargo binstall lilyco-binfmt            # 暂时只会回退源码（该 crate 未发布，见 docs/INTEGRATION.md §0）
cargo install --path lilyco-binfmt      # 源码编译
cargo run -p lilyco-binfmt -- identify --path /usr/bin/ls --json
```

---

## 命令一览

| 命令 | 安全级 | 干什么 |
|---|---|---|
| `identify` | **T0** 只读 | 靠文件自己的字节说清是什么族、什么格式，并摊开它头部自报的字段 |
| `entries` | **T0** 只读 | 列压缩包/归档的成员表：ZIP（含 APK/JAR/DOCX/EPUB）、tar、ar（含 .deb） |
| `regions` | **T0** 只读 | 把整个文件按区上色：头部 / 表 / 代码 / 数据 / 只读 / 元数据 / 空闲 / 尾部叠加 |
| `symbols` | **T0** 只读 | 按分析器的读法看一个目标文件：节表 + `.symtab` 与 `.dynsym` + 地址到名字 |
| `office-info` | **T0** 只读 | 这份办公文件是什么（OOXML / ODF / 复合文档 / RTF）、谁写的、有没有宏与加密 |
| `office-text` | **T0** 只读 | 文件里写了什么：docx 按段、pptx 按页与备注、xlsx 与 **ods** 按格子（表名 + A1 位置 + 值类型）、odt/rtf 各按自己的段落口径；docx 还读批注 / 脚注 / 尾注 / 页眉 / 页脚那几个部件，odt 的页眉页脚在 `styles.xml` 的 master-page 里，odt 的批注嵌在正文段里面，rtf 的页眉与正文混在同一个流里（靠 `\header` 那些目标群分开），每条都带 `from`/`part`/`author`/`date`；rtf 的注是同一件事的第三种存法，也是最坑的一种 —— LibreOffice 把脚注与尾注**都**写成 `{\*\footnote …}` 群，尾注只多一个群里的 `\ftnalt`，而 `\*` 的本意是「不认识后面那个目标群才整群跳过」，一见 `\*` 就跳会把整份文件的注全丢掉（`notes-end.rtf` 就是这条的证据；`{\*\ftnsep\chftnsep}` 那两条是注的排版定义，不是一条注）；rtf 的**批注**又是另一种分法：作者那一格在注之前（`{\*\atnauthor 名字}`），正文在 `{\*\annotation …}` 那一格里，所以两条列表按文件的顺序配、两个条数各交一份（配不上时看得出来），注自己带一个号（与锚区两头 `atrfstart` / `atrfend` 同一个数，`anchor` 就是它）；那一群写的 `atndate` 两份件都对不上同一批字的 docx 里那个 `w:date`（按 epoch 解一个得 2042、一个得 2025），所以 `date` 交 null、原样那串交在 `date_written` 里；第二个作者的中文名「刘奇」是 **LibreOffice 自己的 RTF 导出写不出来**（成了两个问号），而它自己的 docx 导出照抄 —— 两家各按各的文件交；**pdf 也答这一问**（`kind=pages`）：内容流解压、字形码经 `/ToUnicode` 认字、行按文本位置重排（`BT` 复位矩阵、前进量只走 x、`TJ` 那个数符号相反），所以中文标题回来的是字句而不是被排乱的字序；加密的那份不给正文，只说解不出来；**格子的字按文件写的原样交**（xlsx 与 ods 那一族）—— `  两头有空格  ` 首尾那两个空格、开头那个制表符都在，因为 LibreOffice 自己把同一份件导成 CSV 就是这么交的，此前两份读者一起把它们 trim 掉了；段与页那一份仍按排版空白 trim（那里的首尾空白不是内容，而一格的字是）；**把结构搬进 markdown**（`--markdown` 才交那一本 `markdown`，两族走这一本：OOXML 的 word 与 ODF 的 odt）：标题层级、两级列表、表格、站外链接、图、粗斜、段内硬换行逐条搬，空段与分页符段不进（条数交在 `empty_dropped`），`text` 之外还交 18 块 / 10 段 / 2 标题 / 5 个列表项（3 圆点 + 2 编号）/ `list_from_style` / `unresolved_fmt` / `tables` / `table_rows` 这些数 —— 两家生产者的 docx 渲染 359 个码位一字不差，而账本分得开「号写在样式上还是抄到了段上」（5 对 0）；同一份稿子的 odt 那副（LibreOffice 的 docx → odt）块数一样、35 行里只有一行不同 —— 图片地址那一行，各按自己文件写的原样交（`media/image1.png` 对按内容哈希命名的 `Pictures/…`）；ODF 那一族读法整个换：粗斜在 `text:span` 点的**字符样式**上（一跳，可能在 content.xml 也可能在 styles.xml，先到先得；解不到记 `spans_unresolved` 不猜形状）、空格与换行是 `text:s` / `text:tab` / `text:line-break` **记号**（`space_markers` 数还原几枚，而句中那两个空格被生产者拆成「一个字面空格 + 一枚记号」，后半句挂在这个元素的**尾**上 —— 漏了尾就只剩前半句）、层级来自 `text:list` 的**嵌套深度**（docx 那边是 `ilvl` 那个数）、批注与注**就嵌在正文段里面**（`annotations_dropped` / `notes_dropped` 各记一条，它们的字不进正文）、表格里 `number-columns-repeated` 照文件说的数展开；转义只在该转的地方转（表里的竖线、行首长得像记号的正文）。odp / ods / rtf / .doc / .pdf **不交这个键**（这一族还没搬，不是空文档）—— 见事实 109 |
| `office-meta` | **T0** 只读 | 文档属性那份账：`docProps/*`、ODF 的 `meta.xml`、遗留格式的 OLE 属性集 |
| `office-doc` | **T0** 只读 | Word 与 ODT 的结构：段落/标题层级/样式/表格行列/超链接/脚注尾注批注/修订计数（脚注与尾注部件里那两条分隔符都不算注：`notes-foot.docx` 四条 `w:footnote` 读成两条，`notes-end.docx` 三条 `w:endnote` 读成一条 —— 后一份的部件是 LibreOffice 的 docx 导出器写的；ODF 那侧同笔账走 `text:note-class`，`notes-end.odt` 也是 2 脚注 + 1 尾注）；`revisions` 是那份**修订账**——逐条给类型（插入 / 删除 / 改格式 / 移动）、作者、时间、第几段与那几个字，并把相邻同（类型+作者+时间+段+是否段落标记）的元素合成一条逻辑改动（LibreOffice 写 OOXML 会把一次插入拆成两个 run，元素数与条数因此不同，而它自己导出的 ODF 就是一个 region —— 这条合成规则是拿同一批字的三副账对出来的）；ODF 那侧的删字住在 region 里、插字住在正文那两个标记之间，两处都读；`text:tracked-changes` 里那份段不算正文（那是被删掉的字）；遗留 .doc 给 null，红线条在表流里；`contents` 答「有没有目录、收了几级」—— 三家存法没有共同点所以各报各的：OOXML 是 `w:sdt` + `docPartGallery="Table of Contents"` 那层壳**加上**域指令文字里的 `\o "1-2"`（壳可能整个没有，只剩域指令，两种都找；LibreOffice 还把引号写成 `&quot;`，不还原实体就取不到值），ODF 是 `text:table-of-content`（名字在 `text:name`、级别在 source 的 `outline-level`），而 LibreOffice 会把十级条目模板全写出来 —— 「有几个模板」不等于「收了几级」，两个数都报；第三种写法是 RTF（`toc.rtf`）：那一条流里既没有壳也没有属性，只有 `{\*\fldinst { TOC \\o "1-2" \\h}}` 一条域，开关前面的反斜杠**成对写**（单个会开出控制字），前瞻解一遍之后交出来的 `TOC \o "1-2" \h` 与 OOXML 那条 `instrText` 逐字相同 —— 所以「几级」这把读取器两家共用一把，而 OOXML 专属的 `galleries` / `sdt` 两键在 RTF 那边干脆不出现（不造假）；三份件一起看：`toc.docx` / `toc.odt` / `toc.rtf`；没有目录报 `present: false`，不是缺键；`statistics` 回答「多少字多少页」—— 自己数的与生产者自报的并排（词只按空白切，所以叫 `words_by_space`）；.odt 那份还连带把生产者自己写在 `meta.xml` 的段落数页数一起交出来；**RTF 这一支答的是「一条流能证明什么」**：段就是 `par` 控制字切出来的行，脚注与尾注按目标群分开数（尾注是群里另带 `ftnalt` 的那一条），此外只交 `pictures` / `embedded_objects` / `skipped_destinations` / `note_destinations` / `page_destinations`，表只交**行数与格子数**（就是 `row` / `cell` 这两个控制字的条数，两份件上与 docx、odt 两副账一字不差；`trowd` / `intbl` 两个原始条数与嵌套表的两个另给），而 `tables` 留 null —— 「连续的 trowd 算一张表」这条规则在一张表与两张表的对照件上试过，两张数成了一张，判不住的推断就不报；域的总条数另给 `fields`；**链接也从这里读**：`{\field{\*\fldinst HYPERLINK "地址" }{\fldrslt 显示文字}}`，看到 `field` 时往群里**前瞻**一眼（不推进游标、不改 skip），所以显示文字照旧留在正文里，地址与同一批字的 docx 那一份一字不差（站外与否按地址含不含 `://` 说，这一族没有关系表可查）；**字体与样式同理**：`{\fonttbl…}` 与 `{\stylesheet…}` 仍然整群跳过（一个字不进正文），只是跳之前把里面的定义读一遍 —— `styles` 是「这个样式被几个段落用了」数出来的（样式表自己那 77 条定义不算使用），`font_list` / `style_definitions` / `font_definitions` 交定义本身；试过把那两群改成「认识的群」整群吃掉，那会让 `skipped_destinations` 从 65 掉到 23，所以不改；条目写 `\fcharsetN`（N≠0）而名字含非 ASCII 码位的，交 `name: null` 加那个字符集号（cp1252 硬解会得出「‚l‚r ƒSƒVƒbƒN」这种看着像字的乱码，文件写的是 Shift-JIS）；**标题看样式名**：段属性里的 `\sN` 到样式表里查到名字，名字写成 `heading N`（`heading` 与数字之间至少一个空格、数字后面不能有字、这个词不分大小写）的那一段才算标题，层级就是 N —— 与 docx（`w:pStyle`）和 odt（`outline-level`）那两本账同一个形状，`notes.rtf` 与 `tables.rtf` 各两级、与同一批字的 docx 一字不差，全用 `Normal` 的 `notes-end.rtf` 交回空数组（这一支看过样式表，「没有」不是「没看」）；`\sN` 与 `\csN` 是两个各自的编号空间，字符样式的同号名字不能顶掉段落样式的；**表格的那张网**（`tables[].grid`）走直接孩子，交「这张表自己几行、每行几个格、每格的字与合并」—— 合并两家写法不同：OOXML 用 `w:gridSpan` 且**不写**被合掉的那一格（同一行 2 个 `w:tc`），ODF 用 `number-columns-spanned` 并把被盖住的那一格照样写成空的 `covered-table-cell`（同一行 3 个格）；纵向合并 OOXML 两头都写（`vMerge` 的 restart / continue），ODF 只在起头那格写 `number-rows-spanned="2"`。所以「这一行几个格子」是**存储的数**，不是页面上那张表的数（出处：`tables-merged.docx` 与它的 `.odt`）；`tables[].rows` / `cells` 是另一本账（`descendants` 数的，嵌套表算进来），两本都交；**那张纸**（`page_setup`）是一份三家对照账：docx 与 RTF 写 twips（1/1440 英寸）、odt 写 `21.59cm` 这种自带单位的串，全部换成 **0.01mm 的整数**（同一条整数式子、逢半进一，不用浮点，免得两个读者在最后一位上分家），每条还带 `written`（文件自己写的那一串），一节一条（`notes-hf.docx` 两节就两条）；odt 里那条只写网格设置的占位页布局不是一张纸，也不占序号；RTF 只交文档级那一条 —— 某一节的覆写住在 `{\*\sectx}` 群里，而这一族不判分节归属；五份件的十四对「文件 × 家」换算出来的纸面都是 21590×27940（Letter），而 `notes-hf` 的四边只有两家一致：docx 上下写 1440 twips，LibreOffice 的 odt 与 rtf 两个导出都写 720 / 1.27cm，三个数都报，不挑一个当准；第二份尺寸的对照件：A4 的短边三家都换算成 **21001**（11906 twips / `21.001cm`），也就是**谁都够不着「210×297」那个整数** —— 所以这一支不报纸张叫什么名字（开了容差就得回答「JIS B5 与 ISO B5 差 4mm 算不算同一张」），把数交出去让人自己认；那份件的横排第二节在 docx 与 odt 里各写一条（`orient="landscape"`、宽高对调），而 LibreOffice 的 **RTF 导出整份一个 `\landscape` 都没有** → 那一副只有一条文档默认的纵向；方向只交文件写了的（docx 与 RTF 竖排时干脆不写 → null，odt 明写 `portrait`）；.doc 交 null（节属性在表流里）；**断点按词边界数**（`structure.break_words` 六个固定键：par / line / page / pagebb / pbb / sect，没出现也交 0）—— 子串数会骗人（`\pard` 含 `\par`、`\sectd` 含 `\sect`），而且只在没被跳过的那一层数（页眉里的 `\par` 不是正文的一段）；换页在这一族有三种写法，Word 那条 `w:br w:type="page"` 在 LibreOffice 的 RTF 导出里是 `\pagebb`（七份 RTF 件的 `\page` 全是 0），所以 `page_breaks` 交三种词的和；**ODF 的换页不写在正文里**，写在段落点名的那个自动样式上（`fo:break-before="page"`，两跳，与 .ods 的数据样式同一类）—— 以前这一条数的是 `text:soft-page-break`（渲染时落下的位置，七份件里一个都没有），四份明明换了页的 odt 就一起报成 0 了；现在 `page_breaks` 走样式那一跳、`soft_page_breaks` 保留原来那条数，`notes` 与 `toc` 两份件三家（docx / odt / rtf）都报 1；`\sect` 是收尾符（最后一节不带），所以只交 `section_breaks`，`sections` 仍留 null；RTF 那一支的批注是 `{\*\annotation …}` 那一群的条数（作者与字在 `office-text` 里逐条交），修订与保护仍交回 null（目录也不再属这一类：那条流里的 `TOC` 域数得清，见上面 `contents`）—— null 是「这一支没看」或「判不住」，0 才是「这份文件没有」；**段落的格式与分栏**（`structure.paragraph_formats` 与 `structure.columns`）是一家一份形状：OOXML 把 `w:jc` / `w:ind` / `w:spacing` 写在**段自己的 `w:pPr`** 上（不解析样式继承；`w:leftChars="200"` 是缩进的第二种单位 = 两个字，于是有一份 `chars_written` 说「这一族的缩进里出现了 Chars」），而 LibreOffice 的 ODF 导出把它们搬进段点名的那个样式里（`text:p` → `text:style-name` → `style:paragraph-properties`），所以这一族每段都上榜、但点名到 `Standard`（住在 styles.xml）的那一段只能交 `resolved: false` 与 `written: null` —— 那一跳不通，不当成「这段没格式」；两家的字面与单位都不同：同一件事 docx 叫 `both`/`right`、ODF 叫 `justify`/`end`，左缩进是 `1701`（twips）与 `3cm`，行距在 docx 是同一个 `line="360"` 配两种 `lineRule`（auto / exact）、在 ODF 是**同一个属性两种单位**（`150%` 与 `0.635cm`），按字数的缩进更是换成 `loext:margin-left="2ic"` —— 所以 ODF 那份属性表**留着命名空间前缀交**（`fo:` 与 `loext:` 是两个东西，折成局部名就会互相盖掉），而 `xmlns` 那类声明不算属性（两家同一口径，标准库的读者也不把它放进 attrib）。分栏同理：OOXML 一节一条 `sectPr/w:cols`（**只有 `w:space` 没有 `w:num` 就是「一栏」的写法**，所以 `count` 是 null 而不是 1），ODF 是一个 `text:section` 点一个 family=section 的区样式，栏在它的 `style:section-properties/style:columns` 里，**每一栏还各写一份 `style:column`**（`rel-width="32767*"` 那种相对数，两栏相加正好 65535），`style:dont-balance-text-columns` 交在 **`dont_balance`** 这个键上（值 `true` 说的是「不要平衡」，键叫 `balance` 就会被读成反话）。RTF 与 .doc 这两族两个键都不交：LibreOffice 的 RTF 导出在**每一段前面**把样式的默认重发一遍（`\pard\plain\s0…\sa200` 段段都有），「这一段自己写了什么」与样式根本分不开，而全文没有一个 `\cols`（两栏在那条流里根本不存在），第二段那个右对齐更是整个没写出来（全文 `\qr` 0 次、`\qj` 与 `\qc` 各 1 次），按字数的那两个缩进也被抹平成 `\li0\fi0`；.doc 的段格式在表流里 —— 键整个不在，不是 0、也不是 null。ODF 那一份与 `structure.paragraphs` **共用同一份段清单**：注坐在带它的那一段里面（`text:p > text:note > text:note-body > text:p`），所以「有几段」有两个数 —— 不落进段里 4、整棵树数 7，而 LibreOffice 自己写在 meta.xml 的 `paragraph-count` 是 7 —— 段格式账取 4（换一份清单 `index` 就对不上），那个 7 原样留在 `statistics.producer` 里。**编号与列表**（`structure.numbering`）问的是「这一段是不是列表项、第几级、那编号从哪来」，而 OOXML 让生产者把这个选择放在三个地方：段自己的 `w:numPr`、段点名的那份样式的 `w:numPr`（Word 的 `List Number` 就是这样，python-docx 照抄 —— `lists.docx` 六段里三段自己一个字都没写）、或者**两边都写**（LibreOffice 的 docx 导出就是：段上补一份、样式里那份留着，于是 `from` 读到 `both`）。解一个号要跳三跳（`w:num` → 它的 `abstractNumId` → `w:abstractNum` → 里面 `@w:ilvl` 对得上的那条 `w:lvl`），而**两本号是分开编的**（实测 `numId 1 → abstractNumId 8`、`5 → 7`），所以谁也不拿号当号用；三跳各自一个布尔（`resolved` / `abstract_found` / `level_found`），一级自己的账也整份交（`numFmt`、`lvlText`、`start`、`lvlJc`，加上那级的 `w:ind` 与 `w:rPr/w:rFonts` —— 圆点那级的 `lvlText` 不是 `•` 而是 **Symbol 字体的 `U+F0B7`**，不带字体名交回去就是一枚看不见的方块）。文件没点的那一级不替它填：模板那九份抽象都是 `singleLevel`、只带一条 `w:lvl`，于是点 `ilvl="1"` 的那段拿 `level_found: false` 而不是第 0 级的数；点一个不存在的 `numId="77"` 的那段拿 `resolved: false`，而**同一份件被 LibreOffice 重写后变成了 `numId="0"`**（numbering.xml 里也没有 0 这一条）—— 两家各用一种方式指向没有的东西，都按写的交。`definitions` 把每一条 `w:num` 与它指的抽象、有没有人点过它（模板给九条，这份文档只用四条）、以及那抽象的每一级都列出来。ODF 换了形状：级别是**嵌套**（`text:list > text:list-item > …`，深度由读者数，套在里面那一层的 `text:list` 常常不写样式名 —— `chain` 交每一层写了什么），指针也还是两处（段样式上的 `text:list-style-name` 与 `text:list` 自己的 `text:style-name`），列表样式的级别**从 1 数起**（`text:level`）而 OOXML 从 0 数起（`w:ilvl`），两边都不换算；LibreOffice 的 ODF 导出把 `text:list-style` 的定义**全写在 styles.xml**、段样式在 content.xml，所以每一条都带 `style_part` / `list_part` 说清是在哪份件里解到的，`in_content` / `in_styles` 数那两堆。样式上写着 `text:list-style-name=""`（空串）的那一段照空串交、`resolved: false` —— 这一族用空名字说「不套列表」，那与「点了一个没有的名字」不是一回事。**RTF 也交这一份账**（`lists.rtf`，LibreOffice 从 `lists.docx` 转的）：号写在**段上**（`\ilvl0\ls4` 这一对，紧挨着还有第二段 `\li360\fi-360` —— 那是段自己说的缩进，与级别的缩进不是一回事，两个都在表里并排放着）；段的号**先落号本**（`{\listoverride\listid4\listoverridecount0\ls4}` 七条，`listoverridecount` 说的是「这一条覆写了几级」，实测 0 —— 它只换个号），号本再点 `\listid`，那一份定义是 `{\list\listtemplateidN …}` 一群，而**它自己的 `\listid` 写在群的最后**（按 `{\list\listid` 去抓一条也抓不到，全文 14 次 `\listid` 两份表各 7 次）；这一族的级上**一个 `\ilvl` 也不写**，所以级别号是「这份 list 里第几条 `{\listlevel`」（读者按顺序给的号），而每份定义九级全写（七份 × 九级 = 63 级），每一级把 `levelnfc` / `leveljc` / `levelstartat` / `levelfollow` 与 `{\leveltext …}`、`{\levelnumbers …}` 按写的字节交（占位记法 `\'02\'00.;` 原样给出，不替它解成格式串）。最特别的一条：**这一族把生产者自己算好的标签也写进了流里** —— `{\listtext\pard\plain  1.\tab}`，所以每条既有 `label`（解出来的字）也有 `label_written`（那一句原样，连前面那个空格）、`label_tab` 与 `label_font`；圆点那一段就是证据：定义里点的是 `\f1`（字体表里那一个才是 Symbol），而标签那一句自己点 `\f7`，两个号各按各的交，不挑一个当准。**同一份文件里 `{\*\listtable` 带星号而 `\listoverridetable` 不带**，所以前瞻两路都要挂上，而 `skipped_destinations` 一个也没因为这个改动而变。最狠的一条跨家族对照：同一段点第二级（`\ilvl1`），docx 那边抽象是 `singleLevel` → `level_found: false`，而 LibreOffice 的 RTF 导出把九级写全 → `level_found: true` —— 同一份稿子的同一个选择在两家文件里的答案就是不一样。另外这一族**几乎每份件都带着整个模板的七份定义**（`notes.rtf`、`para.rtf`、`tables.rtf`… 都是 7 份 / 63 级 / `listed: 0`），`notes-end.rtf` 只带一份 —— 「定义了没人用」在这里是常态。遗留 .doc 仍然不交这个键：那一份的列表在表流里。**这张表多宽**（`structure.table_layouts`）在 OOXML 是三本账：`w:tblPr/w:tblW` 是表自己说的（python-docx 写 `type=auto w=0`，那是「说了等于没说」的一个值；LibreOffice 重写同一份件会换成 `8640 dxa`，还补 `w:jc` / `w:tblInd` / `w:tblLayout` 与一个**空的** `w:tblCellMar`）、`w:tblGrid/w:gridCol@w` 是网格、每一格的 `w:tcW` 是第三本 —— 合并那张实测网格三条 `2880`，而横向合并那一格自己写 `5760`（它盖住的两列之和），所以「多宽」没有唯一答案，三本各交，一也不替另一圆场；行与格只数这张表自己的直接孩子（套在格里的另一张表不算）。twips 用与「那张纸」同一条整数式子换成 0.01mm，跨家族才可比：`4320` twips、`7.62cm`、`15.24cm` 在两家读者都落在 `7620` / `15240`。ODF 没有表级宽度元素：一条 `table:table-column` 可以**顶几列**（`number-columns-repeated="2"` —— 「几条」与「几列」两本账），宽度一跳在 `family=table-column` 的 `style:column-width` 上，而那份列样式与带 `style:width` 的表样式都写在 **content.xml**（与编号那批把定义搬去 styles.xml 正相反），每条都带 `style_part` 说清从哪解到。**格子自己**的三样也在这份账里（每一格 `shading` / `borders` / `valign` 再加一个 `borders_present`）：`w:shd` 与 `w:vAlign` 按写着的属性原样交，`w:tcBorders` **分两个键**交 —— 实测 LibreOffice 把六格**每格都补一个空的** `<w:tcBorders></w:tcBorders>`，于是「元素在而一条边都没有」（5 格）与「元素整个不在」（python-docx 那份）是两件事，折成一个键就把生产者的习惯读成了文档的说法；而值本身一个都没被改（`FFFF00`、`FF0000`、`sz=6` 双线都照抄，只换了属性顺序）。ODF 那一族的格子三样**也在这份账里**，只是每一样都要再跳一跳：`table:table-cell` 自己身上只有 `table:style-name`（LibreOffice 按「表名 + 地址」给每格配一份自动样式：`表格1.A1`…`表格1.C2`，实测六格六份、**全在 content.xml**），值在那份样式的 `style:table-cell-properties` 里 —— 底色是 `fo:background-color="#ffff00"`（小写带 `#`，与 docx 那面的 `w:fill="FFFF00"` 是同一件事的两种写法，两边都按写的交，不归一化），边是 `fo:border` 或四条 `fo:border-<方位>`（一条值里塞着「宽度 样式 颜色」三段；同一条双线 docx 记每根 `sz=6`（八分点），ODF 记三根合起来 `2.25pt` 再逐根写 `style:border-line-width-top="0.026cm …"`），垂直对齐是 `style:vertical-align`。因为 shorthand 也算写在边的名字里，「写了几条边」与「有没有一条真有字」分两个键（`borders_present` / `lined`）：实测六格每格都写了一条边，其中五格写的都是 `none`，所以 `borders_present` 全 true 而 `lined` 只有一格 true。`padded` 说那份样式写没写 `fo:padding-*`（LibreOffice 连默认值也每格写），`cell_styles` / `cell_styles_in_content` / `cell_styles_in_styles` 数样式的两堆，`cells_unresolved` 数「点了名却没有那份样式」的格子。被合并掉的格子是 `table:covered-table-cell`，**另数一本**（`cell_elements` 是两种格子元素总数、`covered_cells` 只数占位的）：实测两种占位写法 —— 横向合并那格连属性都不写（`attrs: {}`、`written: null`），纵向那格还留着样式名；跨了几列几行照原样留在 `attrs` 的 `number-columns-spanned` / `number-rows-spanned` 里，不并进「这一行几个格」。RTF 与 .doc 这个键也不交：前一族的表宽在页面上是排出来的、流里没有那个东西（只有 `\intbl` 与格分隔），后一族的在表流里 —— 键整个不在（这一族的**列表**倒是读了，见上面 `structure.numbering`）。**文档里那张图**（`structure.picture_list` 与 `pictures` / `pictures_with_alt_text` / `pictures_without_alt_text`）在三家写在不同的地方，而**同一件话在一族里就有两处**：尺寸有 `wp:extent` 与 `pic:spPr/a:xfrm/a:ext` 两处，实测两家两处都写而**写的不是同一个数**（`1440000` 换算 4000 对 `1440180` 换算 4001，两处一起跟着重写换），所以两处都交、不挑一个；替代文字有 `wp:docPr` 与 `pic:cNvPr` 两处（一家只写外面那处而里面那处的 `name` 还是**原始文件名** `dot.png`，LibreOffice 重写时把两句都抄进里面那处），锁也有 `a:graphicFrameLocks` 与 `a:picLocks` 两处（一家一份、另一家两份）—— 合成一个「有替代文字 / 锁了纵横比」就把生产者的习惯读成了文档的说法，`descr_written` 另把空的 `descr=""` 与整个没写分开。号是生产者自己排的（同一个 `word/media/image1.png` 在一家是 `rId9`、另一家是 `rId2`），而且**只能在这份件自己的关系表里查** —— 同一个包里 `word/header1.xml.rels` 也敢再来一条 `rId2`，全包查就串台。`wp:anchor`（浮在页上、文字绕着排）那一种**手上没有一个生产者会自己写**，所以拿 `images.odt` 改掉锚点再让 LibreOffice 导出，那条分支第一次有真件可走：摆法在**元素名**上、绕排是 `wp:wrapSquare wrapText="largest"`（名字与属性一处一半）、摆放的 `relativeFrom` 在属性上而**值在孩子的文字里**（`<wp:align>center` 是一个词、`<wp:posOffset>635` 是一个 EMU 数），于是 `position_h` / `position_v` 各交属性、元素名与那元素写的字三份。ODF 换了一套地方：尺寸是**自带单位的串**（`svg:width="4.001cm"`，与那份 OOXML 的 `1440180` 换算到 0.01mm 都是 4001，两家读者同一条整数式子）、摆法在**属性** `text:anchor-type` 上、地址直接在 `draw:image/@xlink:href`（没有关系表这一层）、替代文字从属性搬成**孩子元素** `svg:desc`（`alt_written` 因此问的是元素在不在）。最出人意料的一条：**同一个选择在 ODF 里有两个词** —— `images.odt` 写 `as-char`，而那份 anchor 的 docx 转回 ODF 成了 `char`，两个串各按各的文件交，折成一个词就是替文件说话（同一趟来回还把 OOXML 那侧的 `wp:anchor` 与绕排整个丢了）。RTF 是第三种存法而**逐张也读**，只是形状又是另一个：「多大」一次写在**三种单位**上（`\picw40 \pich24` 是像素、`\picwgoal480 \pichgoal288` 是 twips、`\picscalex472` 是百分比），文件里没有一个字说 DPI，所以像素那两个不换算法、只把 twips 那一对照「那张纸」同一条整数式子换成 0.01mm（`480` → `847`），而把三者乘回去是推算 —— 不交（同一批字的 docx 那边写的是 `1440000` EMU = 4000，正是 LibreOffice 的 RTF 导出把它拆成了「目标 × 缩放」两半）。「这是什么格式」有**两份凭据**：数据前那个控制字（`\pngblip`）与那串十六进制自己带的前八个字节（`89504e47…` → `png`），两个都交，`sig_agrees` 只在两边都说出同一个词时才比（`\dibitmap` 那种没有词干 → null，不替它编一个「不一致」）。替代文字又搬了一次家：住在 `{\*\picprop}` 那张形状属性表里的一对群（`{\sn wzDescription}` 给名字、`{\sv …}` 给值），所以 `props_written` 说那张表在不在、`alt_written` 说那一对在不在，而 `alt` **可以是空串** —— `notes.rtf` 与 `toc.rtf` 那枚模板小图就是这样（写了、说的是空话），于是那里 `alt_written` 是 true 而 `pictures_with_alt_text` 是 0。数据是折行写的，所以读字节那一段跳空白、碰上第一个既非十六进制又非空白的字符才停；前瞻不动游标也不改 skip，所以 `skipped_destinations` 一个也没变。「两种摆法都不是」的那种 `w:drawing` 不造占位记录（这一族另有 `w:pict`，手上没有件可量），缺口由 `drawings` 与 `pictures` 两个数的差自己说。**字符格式是另一本账**（`structure.run_formats`），四份件把同一句话说在四个地方：OOXML 写在**每串字自己**的 `w:rPr` 的**孩子元素**上（`<w:b/>` 是开、`w:val="0"` 与重写后的 `w:val="false"` 是不 —— 「有 b 这个孩子」不等于「是粗体」），而 python-docx 不给没格式那一串写这一格、LibreOffice 重写时给每一串都补一个空的（`with_props` 14 / `props_empty` 0 对 30 / 16，两边 `with_format` 都是 14）；ODF 只在 `text:span` 上点一个样式名，值在一跳之外的 `style:text-properties` 上（`fo:font-weight="normal"` 是这一族说「明确不」的方式，`found_in` 说那份样式住在 content 还是 styles），而夹在两个 span 中间那串字**根本没有元素**（`element` 是 `#text`、`style` 与 `resolved` 都 null —— 没号可查与查不到不是一件事）；RTF 也没有「一串字」这个元素，格式写在**群头**上（`b`、`cf23`、`fs18`、`aul`，否定是粘在词上的 `0`），所以那里 `with_props` 交 null，号要跳文件自己那两张表（23 → `C00000`，字体号查到条目而名字解不动仍交 null），所有群之外（段前缀那一层）说过的控制字另记一条 `words_outside_groups`（这份件 135 条）。**再往上一问：这句话可以只写一个样式号**（`charstyles.*` 四份件）—— OOXML 的 `w:rStyle` 坐在同一个 `w:rPr` 里而定义在 `word/styles.xml` 的 `w:type="character"` 那一条上，于是每一串字多了 `style` / `style_found` / `style_name` / `style_parent` / `style_format` / `style_switches` 六栏，而 `switches` 仍然只算段上自己说的：`Strong` 那一段 `bold_on` 不数它、`bold_from_style` 数它（1 与 1），另一段样式说斜而段上写 `<w:b/>` 说粗，两处各一半（`where_both_spoke` 是 0，不是 2）。号与名不同：`SubtleEmphasis` 这个号的名字是「Subtle Emphasis」；父样式报出来但不跟；主题色按 `themeColor` / `themeTint` 原样交（解它要开 `theme1.xml`，这一族不开）。ODF 那一家没有「段上自己写」这个地方，开关值一律来自样式，所以它的 `bold_on` 是 2 而 OOXML 是 1 —— 两个数各说各的问，不做等号；而这一族会把 span 套 span（外层点 `Emphasis`、内层点 content.xml 的 `T1`），所以每条带 `depth`，且一条 span 的 `text` 只算它直接带的那些字，外层那条交空串。RTF 的字符样式群头写 `\cs34`，那条定义本身是 `{\*\cs34 … Strong;}` —— 定义读取现在会先剥掉那层「不认识就跳」的 `\*` 再解名字（34 → `Strong`、37 → `Subtle Emphasis`），而 LibreOffice 同时把样式自己的 `\b` 抄进群头，所以号与话两处都在。**一串字里到底有什么**是另一问（`contents` / `text` / `refs` / `note` / `breaks` / `instructions` / `field_chars`，加 `runs_with_text` / `runs_with_ref` / `ref_found` / `field_runs` / `notes_in_parts` / `notes_referenced` / `notes_unreferenced`）：`contents` 按文件顺序交出除 `rPr` 以外的每一个孩子，而 `text` 只算 `w:t` 里的那些字 —— 域指令那一串 `text` 是空串、` TOC \o "1-2" \h` 整串交在 `instructions` 上（那串字不在页面上），图与分页符同样是一串字的孩子而它们不写字；注的引用只有一个号，而**号分两本账**：`notes-end.docx` 的脚注 `2` 排在部件第 0 条、尾注 `2` 排在第 2 条，只按号对就会两条都落在第 0 条，所以 `note` 交种类、号、解到没解到与序号，`notes_*` 那三条从部件与正文两个方向各数一遍（这一批件里三条全被引用，`notes_unreferenced` 是 0）；`commentReference` 的号照交，但它不跳注那两份部件（批注住在 `comments.xml`，另有一本账），于是 `runs_with_ref` 是 1 而 `ref_found` 是 0 —— 那一句里的「0」是数过了没有，与「没看」不是一回事；**这一串字是被谁包起来的**（`wrapped` / `wrapped_written` 加 `runs_wrapped` / `wrapped_hyperlink` / `wrapped_ins` / `wrapped_del`）：`w:hyperlink`、`w:ins`、`w:del` 三个壳上有作者、时间、修订号与链接的号，而串自己一个都没有。**同一次插入两家写出两种形状**：`revisions.docx` 一条壳（id `11`、整句「124000 元」），LibreOffice 重写后是两条壳（id `0` 与 `1`，数与单位各一条），全文的号都被重排（`15` → `5`）—— 所以号只列不比（与图的 `r:embed` 同一族教训）。**删掉的字是第五种情况**：那些字写在 `w:delText` 而不是 `w:t` 上，于是那一串 `text` 是空串、`contents` 却照样交出 `delText`，作者与时间还在壳上（两家一字不差：`李四` / `2026-03-06T11:45:00Z`）；同一批字在 `revisions` 那本账里看得见。链接那句「预算制度」在三副件里是三个号（`rId2` / `rId9`）而地址一字未改。没壳的那些串交 `null` 而不是空串。**这一串字里还可以一个字都不写，只说「这里要算」**（`fields.*` 四份件）：`w:instrText` 那一串 `text` 是空串而 `instructions` 交出 ` SEQ 表 \* ARABIC`（反斜杠按写的交），页面上看得见的是另一个 run 里那句**上一次算出来的缓存值**（`1`）；`w:fldChar` 的 begin / separate / end 三种记号各归 `field_chars`，`field_runs` 数「这一串参与了链」。`w:dirty`（「这域脏了，下次要重算」）只在写的那一份里有 —— LibreOffice 把同一份件重导成 docx 时**把这一格整个丢了**，还把日期与页码的缓存值换成它自己算出来的数（`2026-09-24` → 当天、`2` → `1`），指令也多了尾空格、日期格式里的连字符被反斜杠转义（`\@ "yyyy-MM-dd"` → `\@"yyyy\-MM\-dd"`）—— 三份都按各的文件交。站内跳转的地址写在 **`w:anchor`** 上（外部链接写的是 `r:id`），而它对的是 `w:bookmarkStart` 的**名字**、不是那个 `w:id` 号：`link_found` 三条一件 —— `true`（指着真书签）、`false`（点了一个没有的名，这份件里就有一条坏的）、`null`（根本没写 anchor，那与「查不到」不是一回事）；页脚里那第四条 PAGE 不在正文这份账里（`paragraphs` 走的是 body），所以 `field_runs` 是 12 而不是 16 —— 少了哪一条由部件那本账自己说。**ODF 那一族的链接与域也是段里的一条元素**（`fields.odt` / `notes.odt` / `toc.odt`）：`text:a` 与那四种自己算的（`text:date` / `text:time` / `text:sequence` / `text:page-number` / `text:expression`）都各出一条账，带 `own_written`（那一个元素自己写着的属性，前缀留着）、`link_href` / `link_anchor` / `link_found`，并且**与 span 走同一次样式跳**——实测 LibreOffice 给链接点的是 `ListLabel_20_5`，这一族的样式表里真有这么一份（而 OOXML 那份模板里的 `Hyperlink` 是空的：同一句话一族解得开、另一族解不开，两个数都交）；同一件事在 `toc.odt` 里叫 `ListLabel_20_2`，生产者各起各的名。站内跳转的 href 前面一个 `#`，对的是 `text:bookmark-start` 的**名字**（这一族根本不给书签写号），站外的 href 就是地址、没有第二跳，所以那两格交 null 而不是 false。域在这一族是**拆开写**的：OOXML 一句串到底的指令在这里变成 `text:name` + `text:formula` + `style:num-format`，日期另点一份数据样式（`style:data-style-name`）并带一个完整时间戳（`text:date-value`），页面上那句缓存值按写的交（有一份件的页码写着 `0`）—— 重算不是读者的活。`links` / `field_pieces` / `bookmarks_written` / `runs_with_anchor` / `anchors_found` / `anchors_missing` 六本合计；注、软分页与书签本体仍整块跳过（各有自己的账）**第三族答同一个问题**（`fields.rtf`）：书签是 `{\*\bkmkstart 名}` / `{\*\bkmkend 名}` 两群 —— **照样整群跳过**（名字不是页面上的字，`skipped_destinations` 那笔账把它们算在内），只是跳之前前瞻读一次名字，于是 `bookmarks` 交出解过转义的那几个名、`bookmark_starts` / `bookmark_ends` 两条列表各数一遍（没配上自己会说）；站内跳转的地址住在**指令**里（`HYPERLINK "#表锚点"`，解过转义的那一份），所以 `anchors` 是这些名去掉 `#` 之后的样子，`anchors_found` / `anchors_missing` 与书签名对上才算落得地，而 `links[].target` 交的是文件写的那一串原样（`#\u-30616\'3f…`）—— 同一个地址两份凭据，不替它们对上。三族同一份稿子的答案一致：各 1 条指得到、1 条指不到**这一节的页眉页脚是从上面那一节沿用来的**（`structure.header_footers`、`sections.docx`）：一格一格交，每节六格（页眉与页脚 × default / first / even），每格三种答案之一 —— `own`（这一节自己写的）、`earlier-section`（**这一节没写**，沿用上一处写了它的那一节，Word 界面上那句「与上一节相同」）、或 null（前面也没有）。每格带着号、`target` 原样、解出来的部件名、`part_exists` 与 `external`：号在关系表里根本不存在时，`part` 与 `external` 都交 **null** —— 那与「写了 false」不是一回事（读者连它是不是站外都不知道），这一条在 `sections.docx` 里就有真件（`r:id="rId999"` 指着关系表里没有的那一个号）。两个决定「首尾页 / 奇偶页那一格到底显不显示」的开关按写的交：每节一个 `title_pg_written`，`evenAndOddHeaders` 交「元素在不在」与「写的值」（写了而没给值 ≠ 整个没有这个元素）—— `structure.table_headers` 是「这张表的哪几行每页重复」：OOXML 把它写成**行上**一枚无值的 `w:trPr/w:tblHeader`（在场就是重复，所以逐行交一张布尔表 + `contiguous_from_first`，而 `trPr` 的**枚数**另数一本 —— 重写会给每一行补一枚空的），ODF 把它写成**表身上**两个数 （`table:header-rows` / `header-rows-repeated`，没写交 **null**）。同一条稿子三样：手写那份 3 张表标了 4 行、其中一张标在第二行；LibreOffice 重写那份丢掉那枚非第一行的标记并把每行都补 `trPr`（1/2/0/1 → 3/3/3/3）；转 odt 那两个数一个都不写。RTF 那一族不交这个键（它写 `\trhdr`） —— 见 fixtures/office README 事实 89。`structure.tab_stops` 是「这一段上有哪几个制表位」：OOXML 写在段上（`w:pPr/w:tabs/w:tab`，位置是整数 twip —— 3cm 落成 `1701`、9cm 落成 `5102`），ODF 一跳在段点的那份样式里（位置换成带单位的串，而同一条稿子转过来是 `3cm` 与 **`8.999cm`**），「引导符」在 ODF 是 `leader-style` 与 `leader-text` 两个属性合起来的，小数点那一样换成 `type=char`；「定义了哪几个位置」与「按了几下制表键」两本账分开数（同名！），没人点的样式与 `default-style` 各另交一份；第三家在 RTF 里是一条扁平流（`\tx` 逐条、前缀只管紧跟的那一条，样式表那一群按这一族的规矩跳过不数）—— 见事实 90、91。`structure.comment_ledger` 是「这几条批注谁写的、锚在哪一段」：OOXML 内容在部件、锚点在正文，两边按 `w:id` 配（重写会换掉**部件里的先后**而正文不动），ODF 合在所属那一段里、时间不带 Z，而「几段」分正文与批注里两个数 —— 见事实 92。`structure.keep_switches` 是「这一段与下一页怎么接」那四个开关：OOXML 坐在段上（空元素=开着，`w:val="0"` 这种拼法=关着，三种状态分开交），ODF 一跳在段点的样式上，而且孤行控制在那里是 `fo:widows` + `fo:orphans` **两个数** —— 两族形状不折算；RTF 不交这个键（样式表自己就带这些词，归属判不住）—— 见事实 93。`structure.table_styles` 是「这张表套的哪个样式」：OOXML 两本账（`w:tblStyle` 的样式 id + `w:tblLook` 的六个位与一个十六进制缓存，缓存可以被生产者算过、也可以没算），ODF 只剩一个名字且没有 look —— 见事实 94。`structure.line_spacing` 问的是「这一段的行距是多少」：OOXML 那个数（`w:line`）的**单位由紧跟的另一枚属性 `w:lineRule` 决定** —— `auto` 是 1/240 倍、`exact` 与 `atLeast` 是 twip，于是同一份件里「1.5 倍」与「至少 18 磅」写成**同一个** `360`，两枚分开各交才看得见是谁在选单位（不合成一个「行距」字段、不换算、也不拿默认值替没写那一格的段接上）；ODF 一跳在段点的那份样式上，`fo:line-height` 是自带单位的串（`150%` / `0.776cm`，只按串尾分类交 `unit_forms`）—— 来回这一趟有两处对不上：`atLeast` 那一段的四个相关属性在 ODF 里**一个都没写**（那格 null 不是 0），而 docx 里什么都不写的段零在 ODF 点的 `Standard` 样式里写着 `115%`（同一份稿子两个答案，各交各的）；LibreOffice 重写自己那份 docx 时四个数连单位一字没改，只给段零补了一份**没有 `w:spacing` 的** `w:pPr`（所以 `has_pPr` 与 `has_spacing` 是两本账）；RTF 不交这个键（`\sl` 与 `\slmult` 在样式表里成批出现，段自己没写时是继承来的，归属判不住）—— 见事实 95。`structure.para_borders` 问的是「这一段自己有没有说画个框、铺个底」：OOXML 在 `w:pPr` 下面摆**两枚**元素 —— `w:pBdr` 是装边的壳（里面 `w:top`… 各带 `val` / `sz`（1/8 磅）/ `space`（边离字多远）/ `color`，而 `color` 可以写 `auto`），`w:shd` 是底纹（`val` / `color` / `fill`，还能点一枚主题色 `themeFill`）—— 壳可以在而里面一条边都没写，所以 `border_element` 与 `edge_count` 分开数，不折成一个「有没有边框」；ODF 一跳在段点的那份样式上而形状整个换了：`fo:border="0.74pt solid #ff0000"` 一条 shorthand 顶四条边，也可以四条各写而其中三条**明写着 `none`**（这一族说「这边没有」是写出来的，所以 `sides_written` 与 `sides_none` 两个数各说各的），`w:space` 搬成 `fo:padding`，双线还多一份逐根的 `style:border-line-width-*`。实测三处跨家对不上：LibreOffice 重写自己那份 docx 时把那个空壳**整个丢掉**（3 枚 → 2 枚）并把 `auto` 折成 `000000`；转成 ODF 时 `sz=6` 从 1/8 磅换成 0.74pt；而 `w:val="solid"` 那一段的 `fill=00B050` 在 ODF 里成了 `#ffffff` —— 同一个属性在两种 `val` 下不是同一个角色，两份读者都按写的交，不猜哪个对（RTF 不交这个键：段边框住在样式表里，归属判不住）—— 见事实 96 。`structure.text_boxes` 问的是「文档里有几个文本框、框里写了什么」：页面上只出现一次的那些字，在 OOXML 的一份件里可以**存两份** —— LibreOffice 的 docx 导出把同一个框写成 `w:drawing`（DrawingML，尺寸在 `wp:extent` 的 EMU 上）与 `w:pict`（VML，那一份的 `v:shape` 没写 `style`）两个分支，各带一份 `w:txbxContent`，字一模一样 —— 所以 `boxes_total`（几份格子，2）与 `distinct_text_count`（几句话，1）是两个数，合成一个就把同一句话说成两个框；框自己带段，于是「有几段」也有两个答案（`w:body` 的直接孩子 3 段、整棵树 7 段、差的那 4 段在框里），与批注、脚注同一族；ODF 是一个 `draw:frame` 套一个 `draw:text-box`，名字、锚、尺寸与坐标都写在框自己身上而尺寸是自带单位的串（`5cm`），与 EMU 不同一种东西，按写的交、不换算也不互证；LibreOffice 重写同一份 odt 时挂上 `draw:style-name="Frame"`、把 `svg:x` / `svg:y` / `draw:z-index` **三格整个丢掉**、`5cm` 换成 `5.001cm`，而那一句话一字未改；**有帧不等于有框**（`notes.odt` 那张图的 `draw:frame` 里没有 `draw:text-box` → `frames_total` 1 而 `frames_with_boxes` 0）。RTF 不交这个键：那份流里 `SHAPPIE` 与 `\pict` 各 0 次，框这个形状根本不存在，字直接落进正文段落 —— 见事实 97 。`structure.bookmark_pairs` 问的是「这些书签是怎么配对的」：OOXML 的 `w:bookmarkStart` 带 `w:id` **和** `w:name`，而 `w:bookmarkEnd` **只带号**（实测五条止的 `name_written` 全是 null）—— 所以闭没闭只能按号配；断的两个方向各记一本账（`starts_without_end` 与 `ends_without_start`），名字以下划线开头的（`_GoBack`）是 Word 自己塞的光标记号、`hidden_starts` 另数一笔，重名的不合并（`distinct_names` 与 `duplicate_names` 并排）；LibreOffice 重写同一份时把两个断的**整个删掉**（5 起 5 止 → 4 起 4 止）、号整批重排、重名那条改名 `口径_副本_1`，而站内跳转的 `w:anchor` 一字未改；ODF 换成三种记号 —— `text:bookmark` 是一枚点、`text:bookmark-start`/`-end` 才是跨段的一对（两头写名字，按名字配），**同段起止的一对在这一族变成一枚点**，于是「5 起 5 止」与「3 枚点 + 1 对」不是同一个问，谁也不换算成谁；那个改名的副本在 ODF 里写作带空格的「口径 副本 1」。RTF 不交这一份配对账（缺键 = 这一支不再交一次）：那一族的 `\bkmkstart` / `\bkmkend` 条数早就在 `structure.bookmarks` 那本账上 —— 见事实 98；`page_numbering` 回答「这一节的页码怎么写」，两族两个形状：OOXML 一节一条 `w:pgNumType`（`w:fmt` / `w:start` / `w:chpNum` 各可缺，元素在场与属性写了什么是两件事，而实测 LibreOffice 重写时留着前两条、把 `chpNum` 整格丢掉），ODF 一版式一条 `style:num-format` / `style:page-number`（词汇与 OOXML 不是一套，`upperRoman` 在这一族写成一个字母 `I`），两边各交总数、不比条数也不折算；跨族走一趟「从几开始」两头各丢一次（`w:start` 与 `style:page-number` 都变成没写，null 不是 0 也不是 1），RTF 与 .doc 不交这个键；`languages` 回答「这份文档写了哪种语言」，两族两个形状：OOXML 是一枚 `w:lang` 带三个可以各自缺的属性（`val` 拉丁那一路、`eastAsia` 中日韩那一路、`bidi` 复杂脚本那一路），分四层各交一份（`docDefaults` / 样式 / 段 / run，`levels_seen` 说哪几层写了），三个属性各自的 distinct 是三个清单、不并成「几种语言」；ODF 只有一格语言位且拆成 `fo:language` + `fo:country` 两段（外加 `fo:script`），宿主只认 `style` / `default-style` 的直接孩子，另交 `not_under_style` 一份。实测：模板在 `docDefaults` 上写 `bidi="ar-SA"` 而正文一个字没说；LibreOffice 重写会给三个样式各补一条；转成 odt 时只写 `eastAsia` 那一串字整个没落；`fo:language="none"` 是「说了没有」，与整族零条（`elements_total` 0）是两件事；RTF 与 .doc 不交这个键；`note_settings` 回答「脚注与尾注怎么编号」：OOXML 的 `w:footnotePr` / `w:endnotePr` 自己不写属性，值在孩子身上（`w:numFmt` / `w:numStart` / `w:numRestart` / `w:pos`），另有两个带 `w:id` 的孩子是分隔符引用，而同一枚元素**可以在 `word/settings.xml` 与每一条 `w:sectPr` 各写一份**，两份说的可以不一样 —— 实测 settings 那份说了 `numStart=5` + `numRestart=eachPage` 而节里那份只说 `pos` 与 `numFmt`，于是 `attrs_only_in_settings` / `attrs_in_both` 分三个键交，不合成；LibreOffice 重写时两处的这两格都没了。ODF 是一类注一份 `text:notes-configuration`（实测两份都在 styles.xml），两类注不对称：footnote 那份多写了 `footnotes-position` 与 `start-numbering-at`，endnote 那份没有，没写的交 false；词汇与 OOXML 不是一套（`1`/`i` 对 `decimal`/`lowerRoman`），各按写的交。有注但两处都没写过这一格的原件交 false / 0 与 null，RTF 与 .doc 不交这个键；**哪条批注已解决、谁回复谁**（`structure.comment_threads`）在 OOXML 是**三份部件两跳**：`w:comment` 自己只有 `w:id`，回复与 `done` 写在 `word/commentsExtended.xml` 的 `w15:commentEx` 上，而它用的号是**批注体内那一段的 `w14:paraId`**（第三份 `commentsIds.xml` 又给同一个号配一枚 `durableId`）—— 所以每一跳都单独数「连上几条 / 剩几条孤儿」（`crep.docx` 三条 commentEx 只连上 2 条、三条 commentId 里也有一条孤儿），把三份并成一个「几条批注」就把断线读成了没断。**「没写」与「写了 0」分两档**：`crep-r.docx`（LibreOffice 从「改过一格 resolved 的那份 odt」导出的 docx，这一份的两份部件是**生产者自己写的**）里 2 条注只有 1 条记录，另一条是 `ex_found: false` 而不是 `done_written: "0"`；段号也是它新排的（`01000000`）。词表由此得到独立印证 —— 把 odt 那一格改成 `true` 再导出，LibreOffice 就自己写出 `w15:done="1"`；而 docx→odt 那一路它**不看** `done`（`crep.odt` 两条都写 `loext:resolved="false"`，源件那条 `done="1"` 没落过来）：同一个生产者的两个方向，一个写一个不认。同一份走 docx→docx 则两份部件**整个不见**、连段号都没了（`paras_with_para_id` 从 2 掉到 0）。ODF 只有半句：`loext:resolved` 压在注自己身上（两份件都走，`parts_seen` 说清在哪份解到，`crep-r.odt` 一份件里 `true` 与 `false` 并存），而回复没有对应物 —— 那一族**不交回复那几格**（不是 0）。`comments.docx` 是反面凭据：有两条批注而这一格一个字都没写。RTF 与遗留 .doc 不交这个键 —— 见事实 105；**这一节是从哪儿开始的**（`section_starts`）一节一条，把 `element_present` / `type_written` / `written` 分开交：`nextPage` 是 Word 的默认，所以 `sstart.docx` 三节里只有两节写了 `w:type`（`with_element` 2、`type_missing` 1），而 LibreOffice 的 docx→docx 重写**把第一节那一句补了出来**（2 → 3，两个已写的值一字未变），单节的 `restart.docx` / `notes.docx` 一个都没写 —— 「没说」与「说了默认」是两份文件，各交各的，不拿规范默认替前者补。ODF 不交这个键：转过去之后只有「连续」那一节变成一枚 `text:section`，另两节没有任何写着起始类型的地方，分页改由段落属性点母版页（`style:master-page-name`）承担 —— 同一句问话拆成两处、还有一半没落纸 —— 见事实 108；**文档里的公式**（`equations`）是另一本：OOXML 交每条式子的**挂法**（`m:oMath` 直接挂 `w:p` 是行内、坐在 `m:oMathPara` 里是独立成行）、文件自己写了哪些结构元素（`f`/`num`/`den`/`sSup`/`rad`/`degHide`/`d`/`begChr`/`endChr`/`nor`/`lit`）、`m:t` 里的字，以及**没写就交 null 的对齐**；ODF 那面一条式子住在**另一个部件**里（`draw:frame` 的 `draw:object` 指 `./Object N`，字在 `Object N/content.xml` 的 MathML 里，另有一枚 `draw:image` 指向替位图，两处地址都按写的交），是不是公式凭部件里有没有 `<math>` 根判 —— `math_found` 对 `objects_without_math`，图表那类走的是同一扇门；六条的 `display` 一律写 `block`，所以行内与独立在这一族分不出来；`[n]` 那一条在两族的字分别是 `n` 与 `[n]`（括号在 OMML 是 `m:d` 的属性、在 MathML 是 `mo` 元素），于是 `text_chars` 是 13 与 17，谁也不补谁。LibreOffice 重写会把两条行内式**升级**成独立成行、把作者写的 `centerGroup` 换成 `center`（`align_written_total` 1 → 4），而式子里的字一个没动；从那个 odt 再回转成 docx，账与那次 docx → docx 重写一格不差。rtf / `.doc` / `.ppt` **不交这个键**（那几族是另一套记号，本机没有能写出这些件的生产者）—— 见事实 110；**遗留 .doc** 那一支走的是容器目录：`ObjectPool` 下一物一 storage，正文流的名字按写的交（`Equation Native`）、`objects_total` 与 `equations_total` 各数各的，`\x01CompObj` 里那三枚串交 `label`/`user_type`/`prog_id`；**字在 MTEF 二进制里，没有第二个读者，所以那一族整个不交 `text`**（缺键不是空串），而 piece 表的对象锚与目录的孩子是两处各数一次（`eq.doc` 都是 6）—— 见事实 113 |
| `office-sheet` | **T0** 只读 | 表格的结构：每张表（含隐藏的）与范围、格子与公式、合并格、命名区域、外链；xlsx 每个格子还带**数字格式**（`s=` 是 `cellXfs` 的下标）与换算出来的日期；.ods 走它自己那套（值类型 + 重复计数累加出的格子位置）；`protection` 那份账分两层（工作簿的 `workbookProtection` 与每张表的 `sheetProtection`），两种布尔拼法都认 —— openpyxl 写 `sheet="1" formatCells="0"`，LibreOffice 重写同一份东西写 `sheet="true" formatCells="false"` 并把等于默认的省掉，而 LibreOffice 导出 xlsx 时会把 `lockStructure` 丢成一个空元素（fixture 实测）；空的 `<workbookProtection/>` 报成「元素在场、没说锁」；`.xls` 没有「工作簿一层 + 表一层」那种分法：`PROTECT`(0x0012) / `PASSWORD`(0x0013) / `SCENPROTECT`(0x00DD) 三条写在**被锁那张表自己的子流**里，所以按表交账、`records` 留原值（0x00DD 那条没有第二个读者认得，只给数值、不替它编开关名；0x0013 那格是 Excel 的 16 位旧哈希，不是口令）；隐藏行与隐藏列在 .xls 里是第四种存法 —— 藏在 `ROW`(0x0208) 的 `0x20` 位与 `COLINFO` 的第 0 位（LibreOffice 给这条记录用的是老 id 0x007D），那一位是拿「只改行高」与「只藏一行」两份对照件拆开量出来的，四种写法必须报同一个数）；`comments` 那一份要走两跳才找得到部件 —— 批注不在 `sheetN.xml` 里，而在**这张表自己的关系表**指着的那个部件，openpyxl 放 `xl/comments/comment1.xml`（Target 绝对）、LibreOffice 放 `xl/comments1.xml`（Target 是 `../comments1.xml`），作者名还只是 `<authors>` 列表的下标；ODF 的 `office:annotation` 直接坐在格子里面，所以格子的字要把注的子树跳过（`cell-notes.xlsx` / `-lo.xlsx` / `.ods` 三份件按格子配对逐条对）；`.xls` 是第四种存法，**部件都不换**：一条记录给字（正文偏移 10 自报字数，紧跟的第一条 CONTINUE 首字节是编码旗标 —— 0 一格一字节、1 一格两字节，与 BIFF8 的 `fCompressed` 惯例相反），另一条给「哪个格子 + 谁写的」（住在该表子流末尾），两份列表按出现顺序配、每条带 `whole`、两类记录的条数一起交（`cell-notes-many.xls`：一次只改一个变量 —— ASCII 与中文作者名、2 与 3 个字、带换行的字、`AA100` 两位列名、两张表）；这一族不写作者时间，`date` 一律 null，那两个记录号也不替它们编规范名（MS-XLS 把 0x001C 留给 EXTERNSHEET，这里量到的是「格子 + 作者」）；`--csv [--sheet 名字或序号]` 另交一份 RFC4180 铺平的网格（日期给 ISO、没缓存值的公式格给空；`.xls` 也走同一跳 —— 格子的 `ixfe` 是 XF 记录的出现序号，XF 的格式号在正文偏移 2，自定义号的串在 FORMAT 记录里、内置号查那张内置表，日期基准取 DATEMODE，文件没写 DATEMODE 就交回序列数而不是默认 1900）；.ods 每格还带它继承的**数据样式**（`format_kind` / `decimals` / `format_tokens`，样式分在 content.xml 与 styles.xml 两处）；xlsx 每张表另交自己那份 `print_setup`：`pageMargins` / `pageSetup` / `printOptions` 三个元素各自在不在，没写的那一个是 null 而不是 false（openpyxl 只写边距，LibreOffice 重写同一份东西把十二个 `pageSetup` 属性全写出来，连 `paperSize="9"` 与两个 dpi 都不省）；边距按文件原样交，单位在每张表上说一次（`margin_unit: inch`），不换算法折算成文档那一族的 0.01mm —— `0.5` 与 `0.511811023622047` 正是两个生产者的差；ODS 与 .xls 这一族量过之后**没读**（键整个不在）：五份 .ods 的表元素不点名自己的版式（六份件全量过，一条也没有），只剩 `PageStyle_表名` 那条生产者自己的命名约定，不是规格里的跳；.xls 每张表都写了 SETUP(0x00A1)，可里面的字段位只经过 LibreOffice 自己的来回（改 xlsx 的属性写成 .xls、再改 .xls 的槽位读回 xlsx），十二个属性里只有三个对得上号 |；图那份账要跳三跳（表 → 它自己的关系表 → 画法部件 → 它的关系表 → `xl/charts/chartN.xml`），每张表各自交 `charts` 与 `chart_list`（没有图的表报 0，不是缺键）：一张图交部件名、标题（`c:rich` 的字面量或 `c:strRef` 的引用）、有没有缓存过画的数，每个图组再交类型与全部直接子元素的 `val`（`barDir` / `grouping` / `gapWidth`…），轴 id 单独交 —— 那是生产者自己编的号，两家本来就对不上；**引用串照文件写**：同一段格子在 openpyxl 手里是 `'数据'!B1`、在 LibreOffice 手里是 `数据!$B$1`，同一批文本类目一家写 `c:numRef` 一家写 `c:strRef`，替它们归一化就是替文件编东西；openpyxl 一条 `c:pt` 都不缓存，于是「图里画的是哪些数」判不住、只能交引用，LibreOffice 缓存了并且自报 `ptCount`，自报数与实际点数一起交（`whole`）；ODS 的图也在嵌入对象里：`draw:frame` 上那条 `draw:object` 指着 `Object N/` 那个目录，里面另有一份自己的 `content.xml` —— 于是「表上几张图」这一问要顺着对象走，备注那个 frame 没有 `draw:object` 就不算图，而 frame 的名字连备注框一起数（第一张图可以叫 `Chart 2`）；类型不写在外层而写在每条系列上（饼图这一家叫 `chart:circle`），点数由 `chart:data-point@chart:repeated` 自报（一个元素顶两个点，两个数都交、不展开），范围串是第三种写法（`数据.B2:数据.B3`），还可以指到图自己带的那张 `local-table`（`local-table.$B$2:.$B$3` —— 转一圈回不到表里的数据），这些都原样列在 `chart_list` 里另附一张 `local_table` 的抄本；只有 .xls 还走 BIFF 的对象链，没读；同一张表上的规则另交一份 `rules`：`conditionalFormatting` 一块一套范围（**一条 sqref 里可以塞两段**，照文件交），里面每条 `cfRule` 把写着的属性全交、`type`/`priority`/`operator` 另点名，公式解掉实体交（文件里写 `$B2&gt;200`，读出来是 `$B2>200`），`iconSet`/`colorScale` 里面每个 `cfvo` 的 type 与 val、每个 color 的 rgb 也原样交（同一个白，openpyxl 写 `00FFFFFF`、LibreOffice 写 `FFFFFFFF`）；`dxfId` 是**一跳** —— 规则只写一个下标，字与色住在 `xl/styles.xml` 的 `dxfs` 里，所以一条规则说清「写的是几」「指得到吗」与「那条 dxf 里有哪些元素路径」（`font/b` 这种一层到底），LibreOffice 重写同一条 dxf 会多补 `name`/`family`/`sz`，两家不折成同一个「加粗深红」；`priority` 是各家自己排的号（同一批规则一家 1/2/3/4、另一家 2/3/4/5），所以只交不比；数据验证那份把容器自报的 `count` 与实际条数一起交（`whole`），每条交范围、类型、operator、`formula1`/`formula2` 的原文与全部属性 —— 包括同一条开关的两种拼法（`allowBlank` 的 1 与 true）、LibreOffice 给 list 补的 `operator`、补的 `formula2=0`，还有它把 custom 公式开头那个 `=` 去掉；ODS 的规则住在 number 与 table 样式那一套里、.xls 是 BIFF 记录，两家都没读，所以 `rules` 键整个不在；每张表再各交自己那份 `view` 与 `header_footer` —— 两件人人都问的事：窗口冻在哪里、打出来页眉上有什么。`view` 交 `sheetView` 写着的属性（一家只写四个，`showGridLines="0"` 与 `tabSelected="1"`；LibreOffice 重写同一张表把十五个全写出来而且换成 `"false"` —— 又是那两种布尔拼法，照文件交，不折成同一个布尔）、`pane` 那一个元素（`state` 把「冻住」与「拆分」分在同一个键上：`frozen` 与 `split`）与逐条 `selection`（一家三条、第一条例外不点 `topLeft`，另一家四条人手一份还多写 `activeCellId="0"`）。**重写不是无损的**：同一批格子里 `state="split"` 那个 pane 在 LibreOffice 的 xlsx 导出里整个不见了，冻住的那张照旧留着 —— 这一条是量出来的，不是从规范里推的。`header_footer` 固定交六个段落（odd / even / first × 页眉页脚），每条带 `present`（元素在不在）与 `text`（实体解掉之后的原样串），并把串里 `&L` / `&C` / `&R` 标出来的分段与其余 `&` 码分列 —— `&&` 是一个真的 `&` 不是标记、`&"Calibri"` 是带引号的一整码，所以按码位扫而不是按字符数；LibreOffice 每段前面补一个字体码，两家「写了字的段数」却都是 3。「没写这个元素」与「写了但是空的」也分开算：第三张表 openpyxl 干脆不写 `headerFooter`（present 全 false、text 全 null），LibreOffice 给每张表都写出这个元素而里面是空的（present true、text ""）。ODS 与 .xls 这一族没读（键整个不在）：ODF 的窗口状态与页眉页脚住在页面样式那一套里，**每张表**这一格要的那道跳没有写着的属性可走（六份 .ods 全量过），所以那份账改按页版式交在顶层 `page_styles`（同一份六格账，与 `office-doc` 的 `header_footers` 同一个函数；`style:region-left` / `-right` 两半里的段也算，详见 fixture README 事实 85）；同一问的另一半（这张表打出来是哪几行几列、每页重复哪一行）另交一份顶层 `print_ranges`：OOXML 把它写成 `xl/workbook.xml` 的两条**保留名**（`_xlnm.Print_Area` / `_xlnm.Print_Titles`），归属靠 `localSheetId` 那个**顺序号**（四张表 0..3 全落得地，而 `sheetId` 与 `r:id` 是另外两套各自编的号），一条名字里可以塞几段（逗号分隔，原句与摊开的段都交），LibreOffice 重写同一份时五条一字不差而 sheet 名的引号全消（带引号 5 → 0，`quoted_entries` 单列一本）；ODF 是两处 —— 表身上的 `table:print-ranges`（分隔符换成空白、地址是 `表名.A1:表名.C10` 这种点号写法）与为与 Excel 来回而留的 `table:named-range` / `table:named-expression`（`named_by_element` 是 4 与 1：两段那一样换成了另一种元素），而**重复行那一半只在来回那一份里**（`print-ranges` 永远不写 `$1:.$1`，只读它就把它读丢），五样的 `base-cell-address` 全是同一个（`distinct_base_addresses: 1` —— 归属只在地址串里，不在这条指针上），`range-usable-as` 把「重复行」与「重复列」写成同一串所以按写的交（出处 `print-area.xlsx` / `-lo.xlsx` / `.ods`，见事实 86）；.xls 的那些开关住在 BIFF 的 WINDOW1 / WINDOW2 与 HEADER / FOOTER 记录里，这里没有一个读者能核对；每张表还交 `layout`（列宽行高）、`filter`（筛选那一条）与 `tables` / `table_list` / `table_parts`（表对象）—— 这三份是办公表格里最常见的三个「为什么」：为什么这一列显示不全、为什么这一行不让填不满、这张范围到底是不是一个表格对象。`layout` 交 `sheetFormatPr` 写着的属性（**两家连「默认列宽」写的都不是同一个属性名**：一家 `baseColWidth="8"`，一家 `defaultColWidth="7.7734375"`）、每一条 `col` 原样（同一列一家写 `22.5`、另一家写 `20.47`，藏起来的那列一条写 `hidden="1"`、另一家写 `"true"`，都不折算成同一个数）、「几条 col」与「盖住几列」（按每条自己写的 min/max 加起来，不展开；一条也没有时交 null 而不是 0），以及行的三本账（`elements` 是行数、`with_height` 是写了 `ht` 的行数、`spoken` 是 `ht`/`customHeight`/`hidden` 里说过任何一个的行数，另附说过话的那几行原样，一条也没说过就是空表）。`filter` 交 `autoFilter` 在不在、它的 `ref`、每一条 `filterColumn` 写着的开关与被筛掉的值，还附 `sheetPr` 上那个 `filterMode`（LibreOffice 两张表都写 —— 筛着的 `true`、没筛的 `false`，openpyxl 一个都不写 → null）。`tables` 那一份要跳一跳：`tableParts` 只写关系 id，真部件在这张表自己的关系表里（表对象与图同一类链接法）；每个部件交它写着的属性（`name` 与 `displayName`、`ref`、`headerRowCount`，LO 另补 `totalsRowCount`/`totalsRowShown`）、`tableColumns` 自报的 `count` 与实际条数（`whole`）与**列名本身**（openpyxl 拿范围第一行的字当列名，于是第二列就叫 `10` —— 那是文件自己写的，两家一字不差），还有它自己那一条 `autoFilter`：**表对象的范围与表上筛选的范围可以不一样**（同一张表上 `A1:B3` 与 `A1:C3` 并存），所以两个都交，不替它们挑一个。ODS 的 `layout` 是第三种形状，而这一份账存在的理由正是它：`table:table-column` / `table:table-row` 元素**只写「我顶几个」**（`number-columns-repeated` —— 实测六份 LibreOffice 转出来的 .ods，每一张表都被这样补到整 16384 列）与偶尔一句自己的 `visibility`，宽与高住在它点名的那份自动样式里，所以要跳一跳。于是那一条轴上交 `elements`（几条元素）与 `spans`（盖住几列/几行），而表自己的 `columns` / `rows` 只数**有内容**的那几格 —— 三本账谁也不替谁圆场（`chart.ods` 那张表：5 条行元素盖住 20 行，而有内容的只有 3 行）；`resolved` / `with_size` / `optimal` / `spoken_visibility` 四个数按**全部**元素加，逐条账本另有一个上限（`listed` 是账本存下的条数、`shown` 是这次交出的条数，与 `--limit` 各说各的）；逐条交它点名的样式名、那份样式找着没有、文件自己写的那一串长度与按同一条整数式子换出来的 0.01mm（`unit` 一张表说一次：`1.672cm` 与 `0.529cm` 落进 `1672` 与 `529`）、样式自己的 `use-optimal-*`（实测**行**两根都写、**列**一根都不写，所以那两栏的数不一样）、样式上的 `visibility`（与元素上那句分开放 —— `hidden.ods` 的藏起来的三列是**元素自己**说的，样式那边一个都没写）与 `parent-style-name`。**父链故意不顺**：六份件一个都没写，留这个名字只为让「样式找着了却没有尺寸」这件事自己说清。表元素自己可以写的那四个数（`number-columns` / `number-rows` / `default-column-width` / `default-row-height`）固定交四个键，没写的交 null —— LibreOffice 四个都不写，而「没写」不是「0」。`filter` 与 `tables` / `tableParts` 是 OOXML 才有的东西，.ods 那两个键整个不在；.xls 这三份同样没读；**结果不是数的那几种一律按文件写的交**（`errors.xlsx` / `-lo.xlsx` / `.ods`）：`t="e"` 的 `<v>` 本身就写着 `#DIV/0!`，此前两份读者一起在它前面加了一句 `#错误 ` —— 那串字不在任何文件里，而这个分支从来没被走到，因为手上没有一个会重算的生产者（现在有了，另与 LibreOffice 自己的 CSV 导出逐格对过）；`t="str"` 是公式算出来的那句字（不走共享字符串表），`t="b"` 文件写 `1` 而显示 `TRUE`；每一格另交 `kind_written` 说「这个 `t` 是文件写的还是按规范默认的」（openpyxl 六个公式格一个都不写，LibreOffice 九格全写），每张表另交 `error_cells` / `string_result_cells` / `cells_with_written_type` 三本账；ODF 是第三种摆法：错误格写 `office:value-type="string"`（不是 error）配一个**空的** `office:string-value`，显示的那串只在 `<text:p>` 里，而 LO 另写的 `calcext:value-type="error"` 不跟；最要紧的一条跨家对照：同一个坏掉的 `VLOOKUP` 在 LO 的 xlsx 里缓存成 `#VALUE!`、在它的 ods 里写成 `错误:502`，三个名字各按各的文件交，不替它们对上一个；**日期那一族另有一件真写了 `date1904`**（`epoch.xlsx` / `epoch-lo.xlsx`）：序列数与日期之间没有唯一的换算，1904 基准下 `40169` 是 2013-12-23（比 1900 基准整套挪 1462 天），而「序列号 60 = 不存在的 1900-02-29」那个闰年 bug 特例**只许在 1900 那一边生效** —— 这一件里同一号在 1904 基准下是好好的 1904-03-01；两家写这个开关又是两种拼法（`"1"` 与 `"true"`），LO 重写时还把带时刻那格的小数从 15 位截到 10 位（换算出来的秒不变），那格像日期的字则从 `t="inlineStr"` 换成了 `t="s"`；**一个格子的字还可以分成几段**（`rich.xlsx` / `-lo.xlsx` / `.ods`）：openpyxl 的富文本只写**行内串**（整个文件一条 `sharedStrings.xml` 都没有，`<is><r><rPr><b val="1"/>…</rPr><t>重要</t></r>`），LibreOffice 重写同一份时把八次引用全搬进字符串表并自报 `count="8" uniqueCount="7"`（同一条「甲」被两个格子用了，两个数都是对的，`sst_unique_matches` 只说自报数与条数对不对得上）；每格交 `runs`（逐段：`element`、`text`、`space`、`props_written`、`props_attrs`、`format`）与 `run_total` / `rich_string` / `space_preserved`，每张表交 `cells_with_runs` 与 `cells_with_preserved_space`。格式住在 `rPr` 的**孩子元素**上（实测两家在 `rPr` 自己身上一个属性都不写），同一个开关又是两种拼法（`val="1"` 与 `val="true"`），而「这一段没写 `rPr`」与「那段 `rPr` 是空的」分两件事（同一段稿子一家不写、另一家人手一份）；分段数还是生产者的决定 —— LO 会按字体 fallback 把一格切成两段，所以只交不比。**最要紧的是那两个空格**：`<t xml:space="preserve">  两头有空格  </t>` 首尾那两格此前被两份读者一起 trim / strip 掉（又一个「两个读者一起错」的例子），凭据是生产者自己 —— LibreOffice 把 xlsx 与 .ods 各自导成 CSV 都交回 `'  两头有空格  ,'`，连开头的制表符也一样在，所以这一族现在一律按文件写的字面交，不 trim。`.ods` 是第三种写法而且是**记号不是字面**：空格写作 `<text:s text:c="2"/>`（一个记号顶两个空格）、制表符 `<text:tab/>`、段内换行 `<text:line-break/>`，不展开就一个字符也读不出来；每一格另交 `spans`（几段点了字符样式）与 `specials`（那三种记号几个），而 `text:span` 点的那份样式**不追**（没有量过那一跳的第二个读者）；**「这格是粗体吗」再跳一跳**（`styled.xlsx` / `styled-lo.xlsx`）：格子上的 `s=` 是 `cellXfs` 的下标，那条 `xf` 自己只写 `fontId` / `fillId` / `borderId` 三个号与一串 `apply*` 旗标，字面在 `fonts` / `fills` / `borders` 三张表里 —— 每格交 `style_attrs`（那条 xf 写着的属性原样）、三行表内容（`style_font` / `style_fill` / `style_border`，各自的属性 + 孩子元素，而**孩子再往下带一层** —— 底色不住在 `patternFill` 自己身上，住在它的 `fgColor` / `bgColor` 孩子上，只走一层的话「这格什么颜色」在文件里明明写着而交不出来）、`style_alignment`、三个号本身（写了个指不到的号时行是 null 而号照交）、`style_found` 与 `style_written`（`s` 没写与写了 `s="0"` 是两件事，一家每格都写、另一家默认那一个不写），三个开关分三种形状：粗体是**孩子元素**（`<b val="1"/>` 与 `<b val="true"/>`，元素在而没写 val 按 true 算）、换行是**属性**（`wrapText` 没写是 null，不按默认 false 算）、底色是 `@patternType`；四张表自报的 `count` 与实际条数一起交在 `workbook.styles` 里。重写在这里最看得出不无损：`indexed="64"` 回来成 `rgb="FF000000"`、`lightGrid`+`FF00B050` 被换成 `solid`+`FF90DDB3`、`cellStyleXfs` 从 1 条变 20 条、字体 5 份变 9 份，而空占位 `<patternFill/>` 与 `patternType="none"` 是两种写法（都算「没底色」，原因在原样账里）。`.ods` 的长相在它点名的单元格样式里、`.xls` 的在 BIFF 的 XF 记录里，两家都不交这些键；**公式那枚 `<f>` 自己写了什么**（`formula_elems`，xlsx 与 ods 各一份）：Excel 把一列里长得一样的公式存成一份共享组 —— 主格写 `<f t="shared" ref="B1:B8" si="0">A1*2</f>`，跟随的七格只写 `<f t="shared" si="0"/>`，**正文是空的**，所以「几格有公式」16 与「几格写了正文」9 是两个数（合成一个就把「文件没写这条公式」读成「这格没公式」）；`attrs_seen` 按**文件里属性写的顺序**交（`t` / `ref` / `si`），另给每个属性值的分布。三个生产者三种写法：openpyxl 一枚属性都不写，LibreOffice 重写时**不用共享组**（16 枚各写正文）而给每枚写 `aca="false"`，同一份转成 ods 之后公式变成格子身上的 `table:formula`、16 条全带正文**且逐行平移**（`of:=[.A2]*2`）—— 那正是 LibreOffice 按 Excel 语义把共享组解开了替我们算了一遍，所以 ODF 那一族**不交 `si` / `ref` / 共享那几格**（缺键 = 没有那个位置）。带公式的是**格子自己**，所以那一族的 `attrs` 交的是那一格写着的属性全表、`sheet` 交它所在那张 `table:table` 写的名字（实测 `表一` / `预算表` / `错误`，样式名单独占一格 `style`），而 `cell` 逐条 null —— 这一族不写格子地址（列可以整个不写、行可以用 `number-rows-repeated` 顶好几行）；正文前缀的分布在 `formula_prefixes`（xlsx 那一族没有「前缀」这回事，那个键在那边整个不出现）。还有一档必须分开：`<v>` 在不在（`cached_written`）与有没有缓存值（`cached` 按写的串，openpyxl 那 16 枚带的全是**空标签**）—— 不把「有标签」说成「有值」。这一本不展开共享组、不重放公式语义；遗留 `.xls` 不交这个键 —— 见事实 106 |
| `office-slide` | **T0** 只读 | 演示文稿的结构：放映顺序、每页标题与**演讲者备注**、版式与母版、尺寸与媒体；pptx 与 odp 各按自己的层级走（odp 的尺寸要绕 master-page 那一跳）；遗留 `.ppt` 是一棵 PowerPoint 97 记录树，按 `recType 0x03EE` 的容器归页（一页一个，各页的行、原子数与那条记录在流里的偏移都交出来）—— 这条归属是拿同一份文件的 pptx 逐张对出来的，规范文本手上没有所以只报数值，`SlideContainer`（11 个）那种名字不拿来当页数；页上的图只认**页自己的关系表**（`Type` 结尾是 `chart` 的那几条），不按目录数 —— LibreOffice 往 `ppt/charts/` 里另塞 style 与 colors 部件，按目录数会数出六张而实际两张；一张图交部件名、标题、有没有缓存过画的数，每个图组交类型与直接子元素的 `val`、轴 id 单列（两家各自编号，python-pptx 还写负数）与每条系列的引用串和缓存点；**引用串跨家不可比**：python-pptx 写的是图自己那张内嵌工作簿的引用（`Sheet1!$B$1`），LibreOffice 的 pptx 导出在同一个位置写字面量（`label 0`、`categories`、`0`），而缓存的数一字不变 —— 所以两份都交，不替它们对成一个答案；`paragraph_total`（`p:sp` 里的 `a:p`）与 `text_runs`（`a:t`）分开数：同一家稿子两家写出来段落数 3/1 一样、run 数 3 与 5 不同，把 run 并成段里的字才谈得上比；`size.type` 只交文件写的：python-pptx 写 `screen4x3`，LibreOffice 同样的 cx/cy 把这个属性省掉，那边就是 null，不替它填 "custom"；`.ppt` 那一族的图还住在记录树里，没读，所以它的页上不带 `charts` 键（`.odp` 已经读了，见下面这一句）；ODF 这一家另问一句：图住在 `draw:frame` 里 `draw:object` 指的那个 `Object N/` 目录（备注那个 frame 没有 `draw:object`，于是不算图），类型不写在外层而写在每条 `chart:series` 上（饼图那一家叫 `chart:circle`），点数用 `chart:data-point@chart:repeated` 自报（一条顶两个点），范围串还可以指到图自己那张 `local-table`（`local-table.$B$2:.$B$3` —— 转了一圈之后回不到稿子的数据了），frame 的名字连备注框一起数，所以一页第一张图可以叫 `Chart 2`；这些都原样列在 `chart_list` 里，另附一张 `local-table` 的抄本；**页上那张表**（`table_list`）是第三种合并写法：`a:tblPr` 写着的属性与「这个元素在不在」分两个键（python-pptx 写 `firstRow` / `bandRow` 外加一条 `tableStyleId`，LibreOffice 重写同一张表把它写成一个**空元素** —— 在而没说）、每条 `a:gridCol` 与每条 `a:tr` 的数按写的交（EMU）再交换成 0.01mm 的那一份（`609600` 与 `609480` 都落在 `1693`：重写换了写法没换换算）、每一格交 `a:tc` 与 `a:tcPr` 写着的属性、`tcPr` 里的孩子元素名、`a:bodyPr` 那份**第二处边距**（一家两处都不写，另一家每格补满 `tcPr` 而只往个别格里再抄一遍 `lIns`…）、段数、run 数与用换行拼起来的字（`a:tab` / `a:br` 两边都还原成制表与换行）；`gridSpan` / `rowSpan` 照写的交，而被合掉的那一格**照样在场**（只多一个 `hMerge` / `vMerge`，字是空的、一个 run 也没有），所以「几个格」「跨度之和」「网格几列」是三个数（同一张表 9 / 10 / 3），三个都交、不替它们对成一个。EMU 那条换算有第三方对过：同一张表 LibreOffice 转成的 odp 把列宽写成 `7.62cm` 与 `5.08cm`、行高写成 `1.693cm` 与 `2.54cm`，换算到 0.01mm 与两副 pptx 一模一样（`deck-tables.pptx` / `-lo.pptx` / `.odp`）。odp 页上的表是第三种写法，而这一份账读得动是因为**页上的表与 .ods 里的表是同一种元素**：表住在 `draw:frame` 里，而**表名与位置只在 frame 上**（实测 `table:table` 自己一个字都不写，`written` 是个空对象，而 frame 写着 `name="Table 2"`、`x` / `y` / `width="17.779cm"` / `height`），所以一份账要两份数据。列宽行高走的是 `.ods` 那一条 `layout`（一跳在列/行样式上），跨应用能看出生产者的手感差别：**Impress 不把列补齐**（3 条列元素盖 3 列，而 .ods 每张表都补到 16384 列），并且 `use-optimal-column-width` 在这里**写了**（值 `false`）而在 .ods 的列上一个都没写；同一张表的 frame 宽 17.779cm 与两副 pptx 的 6400800 EMU 换成 0.01mm 都是 17780。合并的第三种写法在这里：起头那格写 `number-columns-spanned` / `number-rows-spanned`，被盖住的那格**另写一个** `table:covered-table-cell`（`covered` 与 `cells`、`merged` 各一本账：实测 7 个有内容的格、2 个占位格、2 处合并）。格子的类型 `kind` 在 odp 这一族**全交 null**：Impress 九个格子元素一个都没写 `office:value-type`，其中七个还是有字的。这一条是两份读者撞出来的 —— .ods 那边一份按「有没有字」推 string / empty、另一份一律兜成 `empty`，只因六份 .ods 里进了账本的格子**恰好全部写了**这个属性才一直没撞上；现在两边都只交文件写了的（`.ods` 那一份账的输出一个字没变，全量对过）。格子点的样式（`ce2` / `ce4` / `ce5`，还有四格什么都没点）只交**名字**：那份样式里写着底色、垂直对齐与四条 padding，但摆法是第三种：住在 **`loext:graphic-properties`**（LibreOffice 自己的实验命名空间，不是 `style:`），而同一份样式里的边（`fo:border`）挂在 **`style:paragraph-properties`** 上，odt 表格用的 `style:table-cell-properties` 在 9 份 table 家族样式里一个都没有 —— 那一跳现在读了：每一格交 `style_props`，两份件里 family=table-cell 的样式都收（实测五份格子样式全在 content.xml，`styles.xml` 一份都没有，而占位格点的 `standard` 是 family=graphic 的另一个东西 —— 只读一份就等于替文件定规矩），三处 properties **各交自己写的元素名**，属性也留着前缀（`fo:` 与 `loext:` 是两个东西，折成局部名就会互相盖掉）。一张表另交 `cell_styles` 那份合计：几格点了名、几格解开、几格根本没点，以及那三处 properties 各有几格 —— 「没点」与「点了找不到」分两笔。每页另交 `links`：两家写链接的地方不一样，OOXML 要跳两跳（run 的 `a:rPr/a:hlinkClick` 只留一个号，地址在这一页自己的关系表里，`TargetMode` 按写的交、没写就是 null），ODF 把地址直接写在字上（`text:a/@xlink:href`，于是没有第二跳也没有「站内 / 站外」那个开关，`external` 与 `id` 都交 null 而不是 false / 空串）；号是生产者自己排的（一家从 rId2 起、重写那份从 rId1 起）而地址一字未变，所以只交不比；odp 那一族不能只走 `draw:frame` —— 实测 python-pptx 的文本框被 Impress 改写成了 `draw:custom-shape`，只认 frame 会把三条链全读成 0，所以现在走「页上除 `presentation:notes` 以外的那几块」（备注里的链不算页面上的链）；每页另交 `relationships` —— 这一页自己那张关系表按文件写的顺序整份列出，内部的 `Target` 解成包内全名、外部的照原样（`links` 因此是这张表的一个子集，不是对它的第二次判断）。那一跳本身是一条踩出来的教训：表住在 `ppt/slides/_rels/slide1.xml.rels`，而成员名按精确匹配找，拼成 `slide1.xml.rels` 就永远读不到 —— 于是这一族从建那天起每页都交**空表**：键在、形状对、值自洽，单元测试一路绿，只有跟第二读者逐字段对账才露出来。同一处还第二次踩了 `r:id`：关系号一定带前缀，按 `attr("id")` 找在那儿拿到的是空串，三条链于是全成了「指不到」；**这一页放映时藏不藏**（每页的 `hidden`）两家各写在一处不同的地方：OOXML 写在页自己的根上（`<p:sld show="0">`，没写这个属性就是看得见 → false，LibreOffice 重写同一份件把那一句照原样留着），ODF 根本不写在页上 —— `draw:page` 只点名一份 family=drawing-page 的样式，值在那份样式 `style:drawing-page-properties` 的 `presentation:visibility` 上，所以要跳一跳，两份件都找（`style_part` 说清在哪份里解到，与 .ods 那一份账一样不赌「自动样式在 content.xml」）。因此 odp 那一页交的不止一个布尔，而是整份 `visibility` 账：`page_style`（页点的名，整个没点是 null）、`style_found`、`visibility_written`（文件写的那一串，找不到那份样式也是 null）与 `hidden`，四条合起来才分得清**「点了名却没有那份样式」（判不住 → null）与「那份样式说了不藏」（false + `visibility_written: null`）**—— `dp1` 那页是后一种。藏起来那页照样在账上（`title` / `links` 一分不缺，放映顺序也不因它变），出处是 `deck-hidden.pptx` / `-lo.pptx` / `.odp` 三份件；踩到的一条：同一份 odp 里 `dp2` 那份样式也写着 hidden 而**没有任何一页点它**，只 grep 全文就会把看得见那页一起判成藏的。别家的页没藏交 false，不是缺键；只有遗留 `.ppt` 这个键整个不交（那一族的隐藏位在记录树里，没读）。**每页那张图**（`picture_list`）在 pptx 只有一处尺寸（`p:spPr/a:xfrm/a:ext`，docx 那族是两处而且两处的数不同）而位置 `a:off` 就在同一层（`360000` → 1000，与「那张纸」同一条整数式子）；**替代文字也只有一处**（`p:cNvPr/@descr`），而那正是坑：python-pptx 在没给替代文字时把**源文件名**写进那个键（`descr="dot.png"`）—— 于是「这页几张图有 alt」数出来是 1，而那句话根本不是描述。所以三个键各说各的：`descr` 按写的交、`descr_written` 只说属性在不在、`pictures_with_alt_text` 只数非空的串，「那是不是人在写的描述」不归读者判。来回一趟 LO 的改写在这里同样看得清清楚楚：号从 `rId2` 变成 `rId1`、形状号从 2 变成 63、`a:picLocks` **整个不见**（locks null）、`<a:stretch>` 还在而里面的 `fillRect` 没了（stretch 从 `["fillRect"]` 变成 `[]`），而尺寸从 `1440000`（4000）换成 `1439640`（3999）—— 位置那一句倒原样留着。不给宽高那一张按 72 DPI 换算成 `508000`（40px），文件里没有任何地方写这个假设 → 只交数、不解释。odp 那一族**复用 odt 那份 frame 账**（3.999cm 落回同一个 3999、地址是 `xlink:href`、替代文字是 `svg:desc` 孩子元素），而 Impress 给图框**不写** `text:anchor-type` → 那一族的 `placed` 是 null（不是 as-char，也不是「没藏」那种 false）；**每个框「字装不下怎么办」**（每页 `autofit`）两族写在两处：pptx 是 `a:bodyPr` 的**独子元素名**（`a:noAutofit` / `a:spAutoFit` 框随字长 / `a:normAutofit` 字缩进框且带着算出来的 `fontScale="75000"` 与 `lnSpcReduction="20000"`），而 python-pptx 在「什么都不做」那一档**一个子元素都不写**（`deck.pptx` 三个框全是空的 `bodyPr`）—— 于是 `says_nothing` 与 `by_element["a:noAutofit"]` 是两本账，不并成一个「不缩放」；odp 要跳一跳（框点名的 family=graphic 样式 → `style:graphic-properties` 上的 `style:shrink-to-fit` / `draw:fit-to-size` / `fo:wrap-option`，三个键三个命名空间，前缀原样留着，折成局部名会互相盖掉），`style_found` / `style_missing` / `props_written` 分得清「点了名找不到」「找到而那条属性没说」；实测 LibreOffice 把 pptx 的 `noAutofit` 与 `spAutoFit` 两档写成**一模一样**的一条（那一档在这次转换里丢了，两行原样摆出来不猜回去），而 `wrap="square"` / `none` 变成 `wrap` / `no-wrap` —— 一句问题两种字面，两边各交各的，只比那一个各家自己答的 `shrinks_text`；只走 `draw:custom-shape`（Impress 把文本框写成这个），备注块的 frame 另数在 `frames`。出处 `deck-autofit.pptx` / `.odp`，见事实 82；**每页每个框「我是什么角色」**（`placeholder_words`，一条一份，按形状顺序）只交 `p:ph/@type` **写着的那一句**：python-pptx 给正文占位符写 `<p:ph idx="1"/>` 而没有 `type`，LibreOffice 重写同一份时写成空元素 `<p:ph/>`（连号都丢），所以那两格交 null —— 规范的默认值（body）与以前两边各猜的一个词（Rust 猜 `other`、读者猜 `title`）都不替文件写进账本；这条分歧以前看不见，因为「占位类别」那条对照只跑在 `.odp` 上（现在对 `*.pptx` 逐份整块比）。形状数与角色账并排放：第 2 页 3 个形状对 `[title, null, null]`（多出来的那一个连 `p:ph` 元素都没有）。ODF 又换地方：角色是 `presentation:class`，「是不是占位符」另有 `presentation:placeholder`，文本框是没有 `presentation:style-name` 的 `draw:custom-shape`，页点哪份版式还写在画页样式上（出处 `deck-ph.pptx` / `-lo.pptx` / `.odp`，见事实 87）；同一处还有**一跳**（顶层 `placeholder_hops`）：页上的 `p:ph` 只留号，字与位置在这一页点名的版式里（要顺页自己那张关系表），对法只有三种、全按写的比（号 / 名 / 两边都空）—— 实测 python-pptx 那份六个形状全对上，LibreOffice 重写那份页上只剩空元素 `<p:ph/>` 而版式写的是 `type="body"`，于是 `hop_found: 3` / `hop_missing: 3`（同一份稿子条数不差，差的是话还在不在），按规范默认值替它接一下就能"全对上"，那是替文件说话，所以交对不上；自制文本框另记 `no_ph`（那一跳根本无从走起，与"走了没对上"不同），`.odp` 与 `.ppt` 不带这个键，见事实 88；**这份文档要点哪些字体**（`structure.fonts`，docx 与 odt 两族，RTF 不交这个键）：OOXML 的三样东西在三处 —— 声明在 `word/fontTable.xml`（每条只写一个 `w:name`）、点在每一格 `w:rFonts` 上（**同一个选择按书写系统写成四个属性** `ascii` / `hAnsi` / `eastAsia` / `cs`，python-docx 的 `font.name` 一次写两遍，于是「几条属性」与「几格说话」两个数：实测 fonts.docx 是 3 个名 / 78 格）、还有一条路不指字面名而指**主题**（`asciiTheme="minorHAnsi"` → `word/theme/theme1.xml` 的 `minorFont/latin@typeface` 才落到 `Cambria`；第四个的拼法是 `cstheme`，小写 th，按写的交）；**写了空话与没写分两笔**（重写那份 styles.xml 里 26 条 `cs=""` → `empty_written`），而「点了而表里没有」在两份生产者手里从 1 个名变成 4 个名（LO 把 `Courier New` 补进表、把两个日文字体从表里去掉），各自按各的文件交；ODF 是**两张键两种指针**：`style:font-face` 声明 `style:name`（表的名字）与 `svg:font-family`（族名，同一张表里带引号与不带的并存、`Cambria` 与 `Cambria1` 同族只靠 `style:font-charset` 分开、另有一条 `name="F"` 族名是空串），而点它的地方分 `style:font-name`（对名字）与 `style:font-family`（对族名）—— 实测同一份件按名字比全落得地、按族名比有 `'Courier New'` 没出现，两个键、两份 undeclared 清单，不并成一数；这张表在两份件各写一份一模一样的 11 条（`faces_duplicated`），所以两份都走、同名先到的算数；**没有一个生产者嵌过字体** → `embedded_refs: 0`（数过了没有）而 `word/fonts/` 另数一本。出处 `fonts.docx` / `fonts-lo.docx` / `fonts.odt`，见事实 83；**同一问的 ODF 那一支**（`structure.header_footers` 的 `family: odf` 那一份）不写在正文也不写在页版式上，而在**母版页**（`style:master-page`）上：六格一份账（`style:header` / `style:footer` 各配 `-first` 与 `-left`），每格两种答案 —— 写了（`present` 加那一个元素自己的属性、几段字、里面每一段的字）或整个没有（null，不是空串），「这一格里有没有会自己算的东西」另数（`fields.odt` 那只有一条 `text:page-number`，缓存的字按写的交）；这一族**没有**「与上一节相同」可交 —— 节点名的是版式、母版页点名自己的版式，而「正文用哪份母版页」在这六份真件里一个字都没写，所以只交 `masters_named_by_section` / `masters_unnamed`，不拿规范里「第一份当默认」那句当文件说过的话。量到的转换损失：LibreOffice 把两节的 docx 转成 odt 时**不写 `text:section`**，改成造第二份母版页（`Converted1`）把第二节那句页眉搬进去，两份还指着同一个版式 `Mpm1` —— 字还在，没人点它的名（出处 `notes-hf.odt`；`paper-a4.odt` 是第二份凭据：两份母版页各指各的版式 `Mpm1` / `Mpm2`，六格一个都没写）。RTF 的书签那一份账这次才真正接进来：`bkmkstart` / `bkmkend` 的名字（前瞻读的，群照旧整群跳过）交 `bookmark_names`，`bookmarks` 那一格交**条数**（与 docx / odt 同一问），跳转那一路 `anchors` / `anchors_found` / `anchors_missing` / `links_external` 由 `Rtf::anchor_ledger` 算一次两边共用 —— 以前这些键只在 `rtf.rs` 自己的 json! 里，office-doc 那条分支从来没放进去，所以探针比的七本书全是 null。出处 `fields.rtf`，见事实 84。`slides[].transition_detail` 是「这一页放映时怎么换」那一份账：`p:transition` 的 `spd`（多快）/ `advClick`（点一下换不换）/ `advTm`（几毫秒换页）是三句可以各自缺的话，而效果是**孩子元素**（`p:fade`、`p:wipe dir="l"`，方向写在孩子自己身上）。最要紧的一条是**一页可以写两条**：`deck-tr.pptx` 第三页一个字没写，LibreOffice 重写同一份时给它补了两条（`{spd:slow,dur:2000}` 与 `{spd:slow}`），于是全篇 4 条元素而只有 3 页有切换 —— 「几条」与「几页有」分两个数交（每页 `elements` + 每条自己的 `written` + 孩子清单），两个数不互推。同一份转成 odp 时页上的 `p:transition` 一个不剩：属性搬去 `style:drawing-page-properties`（`presentation:transition-type` / `duration="PT5S"`），效果搬去一棵 SMIL 动画树（`anim:transitionFilter` 的 `smil:type` / `subtype`），所以 odp 那一族**不交这个键**（缺键 = 这一族没看，不是 0）。两支读者比这一问不比页序（一支按放映序、一支按部件名），按内容多重集与两条求和比 —— 见事实 99。同一问在 odp 记作 `slides[].odp_transition`，而它**写在两处**：页点名的那份 drawing-page 样式里（`presentation:transition-type` / `transition-speed` / `duration` 挨着效果自己的 `type` / `subtype` / `fadeColor`），与页体内一棵 `anim:par` 树里的 `anim:transitionFilter`（`smil:dur` 等）—— 两处都交、不互证；`dp3` 只写半句（没有 `transition-type`、没有 `duration`）就交半句，`dp4` 找到了却一个字不写就交空表（`style_found: true` 而 `written: {}`，与「跳不到那份样式」两件事）。对照 pptx 那一面：同一页在 LibreOffice 的 pptx 重写里被补了两条 `p:transition`，它的 odp 导出对该页一个字不写 —— 两个导出方向相反，各按各的文件交。两支读者按族分键：pptx 带 `transition_detail`、odp 带 `odp_transition`，另一族那个键整个不在；比这一问不比页序 —— 见事实 100。同一页还交一份 `shape_tree`：`p:spTree` 的孩子顺序就是叠放顺序，一条一个形状（`kind` / `name` / `id` / `depth` / `parent` / `xfrm` / `size_written` / `text_carrier` / `paragraphs_direct` / `children`），**树自己那枚 `cNvPr id="1" name=""` 不算形状**，所以「一页几个形状」与「几个 unnamed」都不会被它污染。组合是一个条、孩子用 `parent` 指回它的序号：`deck-gr.pptx` 第 1 页 5 条 = 顶层 2 + 组合里 3，第 2 页那一份清单是空的（`shapes_total` 0 而不是缺键）。组合自己那枚 `a:xfrm` **四份都在**（`off 0,0` 而 `ext` 与 `chExt` 一模一样：页坐标一套、孩子自己的坐标系一套），LibreOffice 重写时号从 2..6 整批重排成 61..65、坐标走那条老换算、五个名字一字未动，还给它补了一个 `spTree` 自己的**全 0 `a:xfrm`** —— 那一份也不进清单，而这正是「找 `xfrm` 只看两层」的理由：往深里找的组合会把孩子的坐标当成自己的。同一页转成 odp：条数与层级一样，可是分组叫 `svg:g`（`draw:group` 一次都没出现）、`id` 这一族根本没有、尺寸是 `5.555cm` 这种自带单位的串，而组合那一层 `size_written` 是**空表而不是 0**；`nested` 两族不同义（odp 里 `draw:frame` 套 `draw:image` 也算一层，`deck.odp` 第 1 页 `nested` 1 而 `groups` 0），两个数不互相解释。新学的一条是 `text_carrier`（字装在哪一层）：pptx 一律 `p:txBody`（一张 `pic` 干脆没有，`carriers_seen` 是空表），而 ODF 同一页里两种并存 —— `draw:frame` 在 `draw:text-box` 里、`draw:custom-shape` 的 `text:p` **直接挂在形状自己身上**（`deck.odp` 第 1 页 `["text-box", "self"]`），只按「有没有 text-box」数段会让这一页从 4 段掉到 3 段、`deck-gr.odp` 那页 4 段全没；段只算自己那一层，组合里那些记在孩子身上，否则一层报一次、整页翻倍。备注那棵树不进这份账（ODF 的白名单里没有 `notes`），遗留 `.ppt` **不交这个键**（缺键 = 这一族没看）—— 见事实 104；**放映里的公式**（`equations`）：一条式子是一个**嵌入部件** —— 页上的 `draw:frame` 里 `draw:object` 指 `./Object N`，字在 `Object N/content.xml` 的 MathML 里，同 frame 还有一枚 `draw:image` 指向替位图（`replacement_found` 问它在不在包里）；`frames_seen` / `frames_in_notes` / `page_thumbnails` / `objects_total` / `math_found` **五格各数各的**，因为 `deck.odp` 一条公式都没有却仍有 5 枚 frame、注块里 3 枚、页缩略图 2 枚 —— 拿 frame 数当公式数就会在这儿说谎；是不是公式看部件里解析得出 `<math>` 根不靠地址猜（图表走同一扇门）；LibreOffice 重写同一份会把 `text:anchor-type` **全丢掉**（2 → 0）、每页补一枚装页缩略图的 frame、样式名 `fr1` 换 `gr1`，还给第二枚对象引用了一张**包里和清单里都没有**的替位图，而式子的字与线性源一字不变。pptx 另起一支（见事实 112）：式子是**文本体里的 OMML**（`a:p → a14:m → m:oMath`）套在 `mc:Choice Requires="a14"` 里，而同一个 `AlternateContent` 的 `mc:Fallback` **把那个形状又写一遍**（`cNvPr` 号与名字一字不差）改挂一张 EMF —— 所以 `shapes_total` / `paragraphs_total` / `formulas` **三格各数各的**（`eqs.pptx` 第一页 2/1/1），`fallback_blip` / `fallback_target` / `fallback_found` 按写的号解到包里；python-pptx 手挂的那一份没有外壳也没有替身图，三格 `fallback_*` 是 null（这一族没写）而字与结构名与 LO 那份一模一样；另记一条生产者边界：裸挂的 `a14:m` 转 odp 会被 LibreOffice 整条丢掉，所以那一支不能拿重写当凭据 |
| `office-package` | **T0** 只读 | 包自证：关系指着不存在的部件、部件没声明内容类型、解压过不了自己的 CRC-32 |
| `office-objects` | **T0** 只读 | 正文之外装了什么：图片、嵌入对象、字体、自定义 XML + 该留心的宏/外链/加密/签名；ODF 没有 OPC 关系表，引用按 `xlink:href` 逐条扫（带 scheme 的才算站外），宏那一条只判 OOXML/复合文档 —— 手上没有含 Basic 库的 ODF 样本 |
| `office-pdf` | **T0** 只读 | PDF 这张对象表：几页（`/Count` 与真的 `/Type/Page` 对账，`/Pages` 是树节点不算页）、每页尺寸与旋转（**页上不写就沿 `/Parent` 继承**）、Info 元数据（PDF 字符串那三件事：转义、八进制、嵌套括号；`<FEFF…>` 是 UTF-16BE）、tagged 标志、字体清单（含 `/ToUnicode` 有没有、这个对象是不是藏在对象流里）、图片清单、以及会自己动的东西（`/JavaScript`、`/Launch`、`/SubmitForm`、`/AcroForm` 字段、`/EmbeddedFiles` 附件、`/OpenAction`、页 `/AA`）。对象靠扫 `N G obj` 认，**不追交叉引用表**，但补了两层：`/Type /ObjStm` 里打包的对象与「没有 `trailer` 这个词、trailer 的键住在 `/Type /XRef` 流字典里」的文件。加密的 PDF 只报结构与加密参数，**不解密**，元数据与 `/Lang` 宁可给 null 也不交乱码。还交「去哪儿」这三份账：`outline` 书签树（`/Outlines` 的 `/First` → `/Next`，子层再 `/First`；每条给标题、层级、目标页对象与它是第几页、根上自报的 `/Count`，`/Count` 的符号就是展开/折叠）、`links`（每张 `/Subtype /Link` 注记分三类：站外 URI、页到页、以及 `/Launch` 那种哪儿也不去的）、`permissions`（`/Encrypt` 的 `/P`，**带符号**的整数，位号从 3 起，R3 才有 9~12 位，R2 的那几位给 null）；加密件里数字与名字不是密文，所以页号与权限位照报，标题与 URI 给 null。`--text` 另交一份正文：按 `/Kids` 的页序、字形码经每张字体的 `/ToUnicode`（`bfchar` 与两种写法的 `bfrange`）、行按位置重排，交回 `{ order_from_page_tree, chars, lines, pages, text }`；**位置这一层有三条规则**，错一条就是「字都认得、顺序全乱」：`BT` 把文本矩阵复位、字形前进只加在 x 上、`TJ` 数组里的数把笔往**反**方向推。`form` 答「这份表里已经填了什么」：`/AcroForm` → `/Fields` → `/Kids`，每条给自己写的 `/T`（只有一截）、沿父拼出来的全名（点号是规范定的拼法，这一串是读者拼的，不是文件写的整串）、生效的 `/FT` 与 `/Ff` 各自带一个「是本条写的还是从父上来的」布尔、`/V` 与 `/DV`（与元数据同一套 PDF 字符串解法）、`/Opt`（规范里这个键**只有数组一种写法**，而数组里两种存法都合法：`[(a) (b)]` 显示值即导出值、`[[导出 显示] …]` 两者分开写 —— 串按写的顺序摊平交回，`options_shape` 再说这一份是 `flat` / `pairs` / `mixed` / `empty`，整个没这个键是 null 而不是空数组）、`/MaxLen`，再加 `kids` / `parent` / `depth` 让分层看得见而不被压平；`value_present` 单列一个键，因为「写了空的 `/V`」与「整个没写 `/V`」不是一回事；`value_shape` 又给 `/V` 的**四种**写法各留一个名字：`string`、`array`（多选列表框：`value` 仍 null，那几段原样交在 `value_parts` 里，不并成文件没写的一个串）、`other`（一个**名字**，勾选框与单选全是这么写的，那一个名字在 `value_name` 里）与 null（整个没这个键）。「勾选框打没打上」分三处各交各的、不合成一个布尔：字段的 `/V`、控件的 `/AS`（`as_state` = 现在显示哪一种）与 `/AP` 里 `/N` 字典的那些键（`ap_states` = 可显示的有哪几种；控件没带 `/AP` 是空表，那与「一种都没有」不同）。单选把 `/V` 写在父上、两个孩子各写自己的 `/AS`，**有一个对不上就照交**，不替它挑一个。不计算 appearance，也不校验签名 —— 只认「`/FT` 是 Sig」与「`/Sig` 这个键在」两件事，与加密件不解密守着同一条界。**口径留着的一句实话**：继承那一段是在 `forms-hier.pdf` 上验的，而那份件**不是编辑器导的**（量过的生产者没一个在父字段上写 `/FT` 或 `/Ff`，只能由 pikepdf 挂出来），所以那几个继承数说的是这份件怎么写，不是某个真表单工具怎么导；编辑器导的那几份上继承计数仍是 0，因为每条字段都自己写了 `/FT`。不含：签名校验、加密文件的正文（内容流是密文）、图形流里的字与 CID 字体的宽度（在 `/W` 里，不跟）；`annotations` 是**整本 `/Annots` 账**（不止链接）：每条交它自己写的 `/Subtype`（整个没写另数一本 `no_subtype`，与写了一个不认识的名字不是一回事）、`/T` 的作者串、`/Contents` 的字、`/M` 那一串（LibreOffice 写的是全零的 `D:00000000000000Z`，原样交，不替它解成日期也不报 null）与注记之间的两条指向（`/Popup` / `/Parent`）—— 一条批注落地是**两条注记**（那条 `/Popup` 也在同一个数组里、rect 还在页面外），所以「几条注记」与「几条批注」两个数各说各的；出处是 `pdf-comments.pdf`，而同一份 docx 用 LibreOffice 默认那一转导出的 `notes.pdf` 里**批注整个不见**（生产者要显式开 `ExportAnnotations`），那份就是 `notes: 0` 的真件凭据；`font_embedding` 是**另一个问句**（`fonts` 问「用了哪些字体」，这一本问「这一层有没有说它带了字面数据」）：逐张交 `subset_prefix`（`EAAAAA+Calibri` 那截前缀，生产者自己截的，没有 `+` 就交 null）、`descriptor`（这张字典自己写的 `/FontDescriptor` 指向哪个对象，没写就是 null）与 `font_file`（descriptor 里那本字典有没有 `/FontFile` / `FontFile2` / `FontFile3`，有则报是哪一个），另给 `with_descriptor` / `descriptor_missing` / `with_font_file` / `subsets` / `file_kinds` / `subtypes`。实测：LibreOffice 那七份每张字体都是 TrueType + 子集前缀 + `/FontFile2` + `/ToUnicode`，`pdffonts` 的 emb/sub/uni 三列全 yes、对象号逐个对上；`risk.pdf` 一张 `Helvetica`（Type1）什么都没说（标准 14 字体从不嵌入），所以 `descriptor_missing` 1 而不是「嵌入失败」。两条边界都量过：`objstm.pdf` 五张字体全住在对象流里（明文只有 17 个对象，不拆第二层就报「一张字体也没有」）；加密的 `locked.pdf` `pdffonts` 一个字都不列而这里数得出 5 张全带 `/FontFile2` —— 第三方闭嘴与能读是两件事。不做的那一件：Type0（CID）的 descriptor 在 `/DescendantFonts` 那一跳之外，本地八份件一张 Type0 都没有，没有凭据就不写那一支（与 `pdf.rs` 早就不跟 CID 的 `/W` 宽度同一条界）—— 见事实 107 |

十三条命令都要 `--path`（`must_exist`）。规模控制两个开关：
`--max-bytes`（默认 `identify` 1 MiB、`regions` 32 MiB、`entries`/`symbols` 与 `office-*` 64 MiB；
**写 0 = 不设上限**）与 `--limit`（`entries` 默认 200、`symbols` 默认 128、
`office-sheet`/`office-package`/`office-pdf` 200、其余 office 命令 100；`regions` 上限固定 256 区；
`office-text` 还有一个 `--max-chars`，默认 20000）。
`--json` 出结构化结果，人读格式在结果超过 500 字节时只报进度——**给脚本和 AI 用请加 `--json`**。

> Web / MCP 端不带这些参数时传进来的是 0（`#[arg(default)]` 只在 CLI 生效）。这两个 0 的含义**不一样**，
> 而且不能混：`--max-bytes 0` = 不设上限（`read.rs::read_blob` 里定的），`--limit 0` 与
> `--max-chars 0` = 按上面的缺省值处理（`opack::take_limit` 里定的）。把 0 当成「只列一条」
> 会交出被悄悄砍短的答案，四端还会给出长短不一的同一张表；反过来把「不设上限」当成缺省值
> 会让一个 8 GB 的文件直接进内存。`office_text.rs` 与 `opack.rs` 的测试各钉了一条。

---

## 快速上手

```bash
lbin identify --path app.apk --json          # 是 APK 还是别的？头字段怎么说？
lbin entries  --path app.apk --limit 200     # 中央目录列出的成员
lbin regions  --path a.out --json            # 哪些字节是代码/数据/表，哪些没人指
lbin symbols  --path /usr/bin/ls --json      # 节表 + 两张符号表 + 地址→名字
lbin --schema                                # 注册表清单（四端同源的那份）
```

`identify` 的两档置信度是**这域的关键设计**：

- `signature` —— 开头几个字节就定死了（ELF/PE/PNG/…）。
- `structural` —— 魔数之外还得让某张表刚好铺进文件才敢这么说。
  例：`0xCAFEBABE` 同时是 Java class 与通用二进制的魔数，只有当架构表每一项的
  偏移与长度都落在文件内、且条数 ≤ 64 时，才叫它 universal binary。

`entries` 每条答案都带 `checks: [{claim, ok, note}]`：文件自报的东西要自己圆得回来。
中央目录说 3 条而实际只有 1 条，就报 `ok: false` 并说清差在哪，而不是默默少报。

`regions` 的 `totals` 必须自洽：`claimed + unreferenced + loaded_unaddressed == 读进来的字节数`。
`gap`（绿色）只表示「没有表也没有加载段点到这里」，**不是「改这里安全」**——校验和、签名、
自读取的程序都不会在表里留下痕迹。没实现的族一律 `mapped: false` + 原因，不编区间。

---

## 覆盖范围（说清楚边界）

| 命令 | 现在能用 | 现在不用 |
|---|---|---|
| `identify` | ELF 32/64、PE、DOS/MZ、Mach-O（thin + fat）、DEX、ZIP/APK、tar、ar、PNG/JPEG/GIF/BMP/WebP/RIFF、TIFF、PDF、WebAssembly、Java class、SQLite、CAB、7z、RAR、xar、EBML/Matroska、ISO BMFF（按 brand）、gzip/bzip2/xz/zstd/LZ4、WOFF/WOFF2、RPM | 内容级解析（只做结构） |
| `entries` | ZIP 家族（方法/CRC-32/两个尺寸/局部头偏移）、tar（typeflag/mode/uid/gid/mtime + 八位校验和自证）、ar（含 `.deb` 的 `` ` ``+换行命名） | 压缩流的解压 |
| `regions` | ELF（节 + 加载段 → 区分对齐填充与真空闲）、PE（段表 + 证书目录）、Mach-O（节表/符号表/间接符号表/重定位，端序由头的字节排列决定）、PNG（块表 + 真算的 CRC） | 其他族给 `mapped: false` + 原因 |
| `symbols` | 走 `object`：ELF/PE/Mach-O/COFF，节表 + `.symtab`/`.dynsym` + 地址到名字索引 | 反汇编、控制流（那是另一个域） |
| `office-*` | OOXML（docx/docm/xlsx/xlsm/pptx/pptm）、ODF（odt/ods/odp）、MS-CFB 遗留（doc/xls/ppt）、RTF；OPC 的关系表与内容类型；OLE 属性集；RTF 的 `\info` 群与 `\*\userprops`；`.doc` 的 piece 表；`.xls` 的 BIFF8 记录（格子按 BOUNDSHEET 偏移归位到每张表）；`.ppt` 的 PowerPoint 97 记录树（文本原子 0x0FA0 / 0x0FA8 / 0x0FBA） | `.ppt` 的按页归位（要 SlideContainer 与 SlidePersistAtom 配对）、宏内容的解析（只检测宏部件）、密码学验证（签名只看有没有，不验签） |

office 这一摊的证据制度在 [`lilyco-binfmt/tests/fixtures/office/README.md`](../lilyco-binfmt/tests/fixtures/office/README.md)：
16 份 fixture 全部由**独立生产者**写出（python-docx / openpyxl / python-pptx / Pillow / LibreOffice），
Rust 测试里的每个期望值都来自第二读者（`scripts/acceptance/office_reader.py` + `lyco_rtf.py` +
`lyco_legacy.py`，只用 Python 标准库）对同一批文件的读取；CI 的 `apps` job 还会把编出来的 `lbin`
与那位读者逐字段对账（`office_probe.py`），不一致就红。

Mach-O 的两处坑已经用真文件钉住（`lilyco-binfmt/src/regions.rs` 的测试）：
节名/段名是**定长 16 字节**，`__compact_unwind`、`__gcc_except_tab` 正好占满、没有结尾符；
`S_ZEROFILL`（`__bss`/`__common`）的 `offset` 是 0，在文件里没有字节，不能画成数据区。

---

## 四端与扩展

```rust
// lilyco-binfmt/src/main.rs —— 与 lilyco-files 同形状
let reg = build_registry_with_policy(policy_for(backend));
lilyco::run_registry_with("lbin", reg, backend)
```

- MCP：`lbin --mcp` → `tools/list` 返回 12 个工具，参数由 `CommandSchema::validate_args` 统一校验
  （缺 `path` 直接被拒，错误信息里带字段名）。
- Web：`lbin --web` → 路径输入框旁边有「…」按钮，点了弹**系统文件选择框**，选完自动回填路径
  （框架能力，见 `readme.md` 的 `/pick`；不用浏览器 `<input type=file>` 是因为它给不出真实路径）。
  对话框可能被压在浏览器后面（Windows 不让后台进程抢前台），所以它会闪任务栏，
  页面上也会写着「看任务栏」，选完提示自动消失。
- 加一条命令：新模块 + `#[derive(App)]` → `main.rs` 的 `for c in [...]` 里加一行，四端自动获得。
- 加一个能画区的族：`src/regions.rs` 加 `xxx_spans()` 并在 `run_regions` 的 match 里加一臂，
  同时补一条「图必须铺满读进来的字节」的测试。

[package]
name = "lilyco-macros"
version = "0.2.2"
edition = "2021"
description = "Lilyco proc macros — #[derive(App)] and #[derive(ValueEnum)]"
license = "MIT OR Apache-2.0"
repository = "https://github.com/lilyco-42/lilyco"
keywords = ["macro", "derive", "cli", "proc-macro"]
categories = ["development-tools::procedural-macro-helpers"]

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"

[dev-dependencies]
lilyco-core = { path = "../lilyco-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
